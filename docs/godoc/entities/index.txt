package entities // import "github.com/LerianStudio/midaz-sdk-golang/v3/entities"

Package entities provides direct access to Midaz API services.

Package entities provides access to the Midaz API resources and operations.
It implements service interfaces for interacting with accounts, assets, ledgers,
transactions, and other Midaz platform resources.

Package entities provides high-level encapsulation for Midaz API interaction.
It provides domain-specific entities like accounts, assets, organizations, etc.

Package entities — internal context bridges.

This file contains UNEXPORTED helpers that delegate to pkg/sdkctx for reading
values that the canonical sdkctx package writes via its public helpers. They
exist because entities/http.go is the consumer of these values during request
preparation, and we want a single in-package indirection point so that swapping
the underlying context-key implementation is a one-file change.

External callers should always use pkg/sdkctx directly:

    ctx = sdkctx.WithIdempotencyKey(ctx, key)
    ctx = sdkctx.WithRequestTenantID(ctx, tenantID)
    ctx = sdkctx.WithoutAutoIdempotency(ctx)

There is intentionally no public re-export of these readers — entities has no
reason to surface context values to user code; sdkctx is that surface.

Package entities provides shared service-entity construction helpers.

Track 7F (audit 7.8) — every entity service implementation has the same two
fields (httpClient + baseURLs) and the same constructor body. The helpers in
this file consolidate that body to one site so the entities stay focused on
per-service business logic.

const ServiceOnboarding = "onboarding" ...
const HeaderTenantID = "X-Tenant-ID" ...
const BoolTrue = "true"
func AddURLParams(baseURL string, params map[string]string) string
func Collect[T any](seq iter.Seq2[T, error], maxItems int) ([]T, error)
func CollectAll[T any](seq iter.Seq2[T, error]) ([]T, error)
func GetWrappedHTTPClient(client *http.Client, _ string) *http.Client
type AccountTypesService interface{ ... }
type AccountsService interface{ ... }
type AliasesService interface{ ... }
type AssetRatesService interface{ ... }
type AssetsService interface{ ... }
type BalancesService interface{ ... }
type Config interface{ ... }
type Entity struct{ ... }
    func NewEntityWithConfig(config Config) (*Entity, error)
type HTTPClient struct{ ... }
    func NewHTTPClient(client *http.Client, authToken string, provider observability.Provider) *HTTPClient
type HoldersService interface{ ... }
type LedgersService interface{ ... }
type MetadataIndexesService interface{ ... }
type OperationRoutesService interface{ ... }
type OperationsService interface{ ... }
type OrganizationsService interface{ ... }
type PortfoliosService interface{ ... }
type SegmentsService interface{ ... }
type TransactionRoutesService interface{ ... }
type TransactionsService interface{ ... }
