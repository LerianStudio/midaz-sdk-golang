# SDK v1+v2 endpoint reconciliation with Midaz develop

**Date:** 2026-08-25
**Branch:** `agent/sdk-v1v2-reconciliation` (worktree `/srv/worktrees/sdk-v1v2-reconciliation`, based on develop @ 4143472; rename to `feat/...` before PR)
**Decision (Fred, 2026-08-25):** the SDK serves BOTH Midaz surfaces — v1 (deprecated but alive) and v2 (the real one) — with explicit version mechanics mirroring the server. No single-version pin.

## Diagnosis (verified against .references/midaz @ d816e289b, develop)

- Midaz built the full /v2 surface Jun–Aug 2026, deprecated ALL of /v1 (`db56dde04`), and **removed from /v1**: CRM (`cede08b1a`), fees (`109e9c1de`), composition (`804825a93`).
- v2 dropped legacy transaction creates (`7150848bd`) — creation is `POST /v2/transactions/direct|hold`, plus `block|unblock`, all top-level (not org/ledger-scoped in the URL).
- v2 dropped asset-rates (`c781f6a97`) — asset-rates is v1-only.
- Billing family moved org-scope → **ledger-scope** in v2 (`/v2/organizations/{org}/ledgers/{ledger}/billing-packages|packages|estimates|billing/calculate`).
- SDK today pins `/v1` on every base URL (`entities/entity.go:normalizeServiceURL`, `config.DefaultLedgerAPIVersionPath`). Consequence: holders, instruments, encryption, billing-packages, packages, estimates, composition, protection/audit **404 against Midaz develop**.
- SDK bugs found on the way: `entities/aliases.go` calls `/aliases`, `/holders/{id}/aliases` — routes that no longer exist anywhere (renamed to instruments, v2-only). **Correction (2026-08-25, review):** the earlier claim that `entities/operations.go` PATCHes a non-existent account-scoped path was WRONG on both halves. The update already targets the transactions-scoped path the server serves (`.../transactions/{txId}/operations/{opId}`), and the account-scoped operation READ (`.../accounts/{id}/operations[/{opId}]`) does exist server-side, deprecated. Epic 2 must migrate that file onto the generated client, NOT "fix" a path that is already correct.
- Tracer plane: SDK spec == server spec, path-for-path. **Zero tracer work.**
- Old branch `sync/monorepo-20251219`: evaluated, discarded (Dec 2025, 442 commits behind, content superseded by v4 remodel + PR #236).

## Mechanics that make this cheap

- Server OAS (`components/ledger/api/openapi.huma.yaml`): `servers: [{url: "/"}]`, version prefix **in the paths**, operationId seam `listOrganizations` (v1) / `listOrganizationsV2` (v2). 197 ops: 87 v1, 110 v2.
- SDK codegen: `api/ledger.openapi.yaml` → `scripts/generate-clients.sh` (specdowngrade 3.1→3.0.3 + oapi-codegen) → `internal/genledger`, with drift gate (`make check-codegen-drift`). Adopting the server spec + regen produces both versioned client surfaces automatically.

## Target public surface (code decision)

`client.Entity.V1.*` and `client.Entity.V2.*` service groups, each exposing only what its server version serves. The asymmetry (asset-rates v1-only; holders/billing/direct-hold v2-only) becomes compile-time visible. Base URLs lose the version pin (server base is `/`). Tracer accessors unchanged. Breaking for v5-beta users — accepted, v5 is beta.

## Epics

### Epic 1 — Spec adoption + regen + pin removal (foundation)
1. Copy `.references/midaz/components/ledger/api/openapi.huma.yaml` → `api/ledger.openapi.yaml`. Verify tracer spec byte-diff (expected no-op).
2. `make generate`.
3. Remove the `/v1` pin in the same stroke (spec paths now carry /v1; keeping the pin would produce `/v1/v1/...`): `normalizeServiceURL` keeps host+optional path, no version append; drop `DefaultLedgerAPIVersionPath`; update `pkg/config` + the three `.env*.example` files + `check-config-parity`. NOTE: the tracer spec has unversioned paths and the tracer serves under /v1 — the tracer base URL must keep its /v1 (pin stays tracer-side or moves into tracer URL default); verify against server routing before choosing.
4. Fix compile fallout in facades by renaming to the new v1 operationIds — wire behavior on v1 preserved.
5. Gate: `go build ./...`, `make test`, `make check-codegen-drift` green.

### Epic 2 — Version groups + legacy repair
1. Introduce `Entity.V1` / `Entity.V2` groups; move existing facades under `V1` (paths now carry /v1 from the spec).
3. Delete `entities/aliases.go` (dead server-side). Migrate `entities/balances.go` + `entities/operations.go` onto the generated client (kills the `transaction`/`crm` base-URL keys and fixes the operations PATCH path to transactions-scoped).
4. Gate: existing v1-family tests green under the new accessors.

### Epic 3 — V2 surface (landed; see "Findings from Epic 3")
1. V2 facades for the dual families (organizations, ledgers, accounts, account-types, assets, balances, operations, portfolios, segments, routes, settings, metadata-indexes, counts).
2. Move the v2-only families to their real home: holders, instruments, encryption, protection/audit, composition under `V2`; billing-packages/packages/estimates/billing-calculate **rescoped to ledger**. (Done in Epic 1.)
3. Transactions V2: reads/patch/cancel/commit/revert under ledger scope + top-level `direct`/`hold`/`block`/`unblock`; model divergences mapped in `models/`.
4. Missing v1 endpoints worth exposing while here: `transactions/block|unblock` (v1), external-code account lookup (already generated, no facade).
5. Cross-cutting: the DELETE-204 defect fixed once at a shared seam.
6. Gate: unit tests per facade; `make ci`.

### Epic 4 — Consumers + verification
1. Examples (`examples/workflow-with-entities`, `examples/mass-demo-generator`) onto the new accessors; `make demo-data` run.
2. Docs: `README.md`, `docs/README.md`, `docs/mapping/{external,internal}_apis.md`.
3. Full `make ci` + `make verify-sdk`; PR (base develop, `feat!:` — breaking).

## Findings from Epic 1 (input for Epics 2-4)

- **Tracer spec HAS drifted** — the diagnosis line "SDK spec == server spec, path-for-path" holds for PATHS only. Schemas diverge: the server renamed `currency` → `asset` in three limit/usage schemas, added an RFC 9457 `upstream` error extension member (+ `Upstream` schema), and returns **201 Created** where the SDK spec still says 200 on three operations. Left untouched in Epic 1 (semantic, not cosmetic, and outside a ledger-scoped epic). Needs its own commit: adopt the server tracer spec, regen, fix the `Currency` field rename (3 refs in `internal/gentracer`) and the 200/201 gates. **Closed in Epic 2 — and it was WORSE than "drift":** the rename is not confined to three response schemas. The Tracer's REQUEST bodies are opaque `format: binary` in its OAS (a Huma artifact), so the SDK's hand-written input models were the only place the request field name lived, and they still said `currency`. Server-side `asset` is `validate:"required"` on all three write paths (`internal/adapters/http/in/limit_validation.go:94`, `pkg/model/validation.go:78`, and `ReserveRequest`, which embeds `ValidationRequest`), and the tracer server has **zero** `json:"currency"` tags left in non-test Go code. So every limit create, transaction validation and reservation the SDK sent was rejected on the wire, and every limit / validation read decoded an empty asset. Six public model fields renamed (`Limit`, `CreateLimitInput`, `ValidateTransactionInput`, `ReserveInput`, `TransactionValidation`, `ValidationSummary`).
- `WithBaseURL` and the environment defaults fan one base URL out to both planes; each fan-out now has to re-shape the tracer copy (`tracerURLFromSharedOrigin`). Any new fan-out path must do the same or tracer calls land one segment short.
- `CreateOrganization` lost its OAS `Authorization` param (now a security scheme). It was always passed empty — the auth round tripper sets the header — so this is a wire no-op.
- Among the v1 operations only `CreateOrganization` drifted in its Go SIGNATURE, but the error CONTRACT changed across all 85 v1 `Parse*Resp` functions: the regen made the server's real v1 error shape explicit, swapping `Error` for `LegacyError` and `application/problem+json` for `application/json`. Request/response wire behavior on the success path is preserved; the error path was NOT — the SDK error decoder only read RFC 9457 members, so every v1 error decoded to "API error with status code N" with field detail dropped. Fixed on the decode side (FINDING 6, `fix(errors): decode v1 legacy error bodies`): the decoder now reads `message`/`fields`/`entityType` when the RFC members are absent.
- The `ledgerId` query filter is gone from `PackagesFilters` / `BillingPackagesFilters` (the ledger is a path segment on v2). Public model change already landed.
- **Correction (2026-08-25, review):** the original line here — "they read the `crm`/`transaction` base-URL keys, which are now bare, so they no longer double-version" — recorded a CRITICAL regression as a non-event. Bare bases did not make those three services correct; it made them UNDER-version. Building paths by concatenation off a bare base emitted `/organizations/...` where the server routes only `/v1/organizations/...`, i.e. a guaranteed 404 across 24 methods (18 balances, 5 operations, aliases). Fixed by stamping `/v1` in the hand-built paths (`fix(entities): version the hand-rolled balance, operation and alias paths`) — a stopgap that retires when Epic 2 migrates balances and operations onto the generated client. **Aliases is NOT fixed by that stamp and cannot be:** the server has zero alias routes at any version, so the stamp only swapped one 404 for another. `entities/aliases.go` stays dead until Epic 2 deletes it. The old tests could not catch it: they injected a base URL that already carried `/v1`, so they asserted a shape the shipped client never produces. Epic 2 must keep the pipeline-level path test (client built from `WithBaseURL` against a live server) when it removes the stopgap.

## Findings from Epic 2 (input for Epics 3-4)

Everything below was verified against `.references/midaz` @ `d816e289b` or against a live `httptest.Server` through the public client.

**Corrections to earlier claims:**

- **The three CRM validators had NO consumer.** The Epic-2 brief asserted `validateCRMOrganizationID` / `validateCRMUUIDParam` / `validateOptionalCRMUUIDParam` were used by the holders/instruments/composition facades. They were not — each had exactly one caller, `entities/aliases.go`. Those facades route over the generated client and validate through it. Deleting the alias service left the whole of `entities/crm_shared.go` dead; it is gone. (`models.validateCRMNullFields` is a different symbol and does survive, via `models/instrument.go` and `models/holder.go`.)
- **The plan's own Epic 2 item 3 was already corrected once and the correction held**: the operations PATCH was always transactions-scoped, and the account-scoped operation READ does exist server-side as deprecated. Neither was "fixed"; both were migrated as-is.
- **The `transaction` base-URL key was a phantom, not just redundant.** After the migration nothing read it to build a request, yet `pkg/config.Validate` and `midaz.setupEntity` both FAILED without it — a mandatory config key with no effect. Removed end to end, along with `pkg/config.ServiceTransaction`. `onboarding` is now the sole Ledger key and `tracer` the sole Tracer key. No `.env` change was needed: `MIDAZ_TRANSACTION_URL` and `MIDAZ_CRM_URL` never existed as env vars, only as internal labels.

**Money-path bugs found while migrating (all pre-existing, all now fixed and guarded):**

- **The balances list is cursor-paginated and the legacy iterator was an unbounded loop.** `components/ledger/internal/services/query/get_all_balances.go:32` calls `filter.ToCursorPagination()`, which (`pkg/net/http/httputils.go:533`) carries only Limit/Cursor/SortOrder/StartDate/EndDate — `page` is dropped on the floor. The old `ListBalancesPages` gated on `Pagination.HasMore()` (true whenever `NextCursor != ""`) and then did `current.Page++`, a parameter the server ignores, so against a real multi-page ledger `ListBalancesAll` re-requested page 1 forever, yielding the same balances indefinitely. `models.BalancesListOpts` therefore had to change shape (`PageListOpts` → `CursorListOpts`); the method signatures are unchanged. Guarded by `TestListBalancesAll_AdvancesByCursor`.
- **`models.BalancesFilters` (AccountID / AssetCode / Status) was never honoured** — no wire slot in the generated params, no server-side reader. Deleted rather than left silently dropping the caller's filter.
- **`models.OperationsFilters` exposed the wrong three fields.** The server (`get_all_operations_account.go:32-37`) filters on `OperationType`, `Direction`, `RouteID`, `RouteCode`; the SDK exposed `Status` and `AssetCode`, which the server parses and then ignores, and exposed none of the honoured ones. `OperationsListOpts`/`OperationsFilters` are now aliases of the `AccountOperations*` pair: one server endpoint, one type, no drift possible.
- **The alias and external-code balance lookups take no query parameters and are not paginated** (`.../http/in/balance.go:197-239` returns a fixed `Pagination{Limit: 10}`; both generated ops have no params struct). Their `*Pages`/`*All` iterators were fiction. Epic 2 first tried to keep them and reject non-zero opts; the fix round deleted them instead — see fix-round finding 2.
- **The point-in-time date validator was too NARROW, not too loose.** It demanded RFC3339 with an explicit offset and rejected `"2026-01-02 03:04:05"` — the exact format the OAS documents for the `date` query. The server accepts RFC3339, `2006-01-02T15:04:05` and `2006-01-02 15:04:05`, and separately requires a time component. The facade now mirrors that set exactly.
- **The RFC 9457 `upstream` member was being discarded by the SDK error decoder.** lib-commons scrubs detail, `errors[]` and code at status >= 500 and lifts `upstream` through that scrub as the single exception (`commons/net/http/problem/install.go:81`), so on a scrubbed 5xx it is the only actionable diagnostic there is. It now lands in `Details["upstream"]`.

**Deliberate decisions (Fred/orchestrator, 2026-08-25):**

- **`WithLedgerURL` does not derive the Tracer base; only `WithBaseURL` fans one origin out to both planes.** Kept as-is: a per-plane option addresses one plane, and `WithBaseURL` is the single fan-out. Recorded because it is an asymmetry a reader will trip over next to the Epic-1 fan-out finding.
- **`onboarding` stays the Ledger plane's key name for now**, but it is a rename candidate (`onboarding` → `ledger`): it is the last place the pre-two-plane service naming survives, and it now names the whole Ledger plane rather than one service. Flagged for the review round, not done here.
- **`entities/observability.go` stays** (415 lines, zero callers, one file-level `//nolint:unused`). It plus the three `t.Skip`ped tests in `business_observability_test.go` ARE the specification that the pre-existing deferred Task 5.2.6 re-homes (`docs/plans/2026-06-30-sdk-v4-remodel.md:621`). `entities/operations.go` was the last business-event emitter, so emission is now zero-coverage across all accessors — which was already the sanctioned state for the other 15.

**Latent, cross-cutting, NOT introduced by this epic — wants one decision across all 15 facades:**

- A single-object response whose `id` is not a UUID fails inside the generated `Parse*Resp` and surfaces as `errors.NewInternalError`, i.e. an SDK internal fault rather than a decode error. Harmless against Midaz today (ids are UUIDs) but a server-side id-format change would degrade into the wrong error class.
- **Every generated DELETE parser unmarshals the body as an error whenever `Content-Type` contains `json`.** A 204 with `Content-Type: application/json` and an empty body therefore returns `unexpected end of JSON input` — a SUCCESSFUL delete reported as a failure. Midaz is safe today because its delete handlers return `struct{}` and Huma emits a bodiless 204 with no content type, but any gateway or proxy that adds one breaks every delete in the SDK.

## Risks
- v2 request/response schema divergences beyond paths (typed fields) surface during Epic 3 — the generated types are ground truth, not the v1 siblings (see plane-clients memory).
- `/v2/transactions/direct|hold` carry org/ledger in the body — contract to be read from the generated types, not assumed.
- Release tooling: v5 module path already handled (#239); no module bump here.

## Status
- [x] Diagnosis + decision
- [x] Epic 1 — **landed** (`ad3e193`). Server spec adopted; the generated client now carries both surfaces (v1 ids bare, v2 ids `*V2`). Ledger base URL is bare, tracer base keeps `/v1` (verified against the server's Fiber `/v1` group mount). The families Midaz removed from /v1 — holders, instruments, encryption, composition, protection/audit — are rewired onto their V2 operations; the billing family (billing-packages, packages, estimates, billing/calculate) is rescoped to ledger and its facades take a `ledgerID`. Gates green: `go build`, `check-codegen-drift`, `make test`, `make lint` (0 issues).
- [x] Epic 1 fix round (2026-08-25, post-review). Seven findings closed, one commit each:
  1. `fix(entities)` — the three hand-rolled services emitted unversioned paths off the now-bare base (guaranteed 404 on 24 methods). `/v1` stamped in; pipeline-level path tests added; the version-blind test injections replaced. This repaired balances (18 methods) and operations (5); aliases stayed broken either way — the server serves no alias route at any version, so the stamp only changed which 404 it earns. Epic 2 deletes that file.
  2. `fix(entities)` — a `/v1` or `/v2` suffix on the Ledger base is now rejected at construction (it silently double-versioned; old `.env` files still carry it). Tracer stamping untouched.
  3. `docs(examples)` — the workflow example no longer instructs the rejected `MIDAZ_LEDGER_URL=.../v1`.
  4. `docs` — base-URL statements corrected in `docs/environment.md`, `docs/comprehensive-architecture.md`, `docs/mapping/internal_apis.md`, `README.md`. Surrounding prose stays Epic 4.
  5. `fix(entities)` — billing calculation and fee estimation reconcile the ledger in the path with the `ledgerId` in the body (fill when empty, reject when contradicting). Money-adjacent.
  6. `fix(errors)` — v1 legacy error bodies (`message`/`fields`/`entityType`) now decode instead of degrading to "API error with status code N".
  7. `refactor(entities)` — the unreachable per-plane base-URL fallbacks at entity construction removed.
- [x] Epic 1 fix round 2 (2026-08-25, final). Four findings closed, one commit each:
  1. `fix(errors)` — a construction failure rendered as "failed to initialize entity API" and nothing else; the actionable cause (which base URL, which variable to edit) only survived behind `errors.Unwrap`. The cause text now travels in `Error()`; the wrapped cause is unchanged.
  2. `fix(entities)` — billing-package and fee-package creation now reconcile the path ledger with the body `ledgerId`, closing the same money-adjacent gap fixed on calculation/estimation in round 1.
  3. `docs` — the `make demo-data` guidance and the CRM-plane statements in `docs/comprehensive-architecture.md` corrected (they still instructed the rejected `/v1` base and a `MIDAZ_CRM_URL` that no longer exists). `docs/mapping/*` still carries `WithCRMURL`/`MIDAZ_CRM_URL` — Epic 4 owns those files.
  4. `docs(plans)` — this record: the round-1 stamp did not fix aliases.
- [x] Epic 2 — **CLOSED** (2026-08-25, after two fix rounds and an adversarial verification round). Landed as 7 commits, `e355c0a`..`731e9c1`, plus 12 fix-round commits and 6 more in round 2 (`47f9e39`..`fc74de3`); not pushed. Gates green: `go build ./...`, `make test`, `make check-codegen-drift`, `make lint` (cold cache, 0 issues), `scripts/check-config-parity.sh`.
  1. `fix(tracer)` + `fix(errors)` — server tracer spec adopted; the retired `currency` wire field replaced by `asset` on three write paths and three read paths; the `upstream` provider error now decodes. See the corrected Epic-1 finding above.
  2. `docs(entities)` ×2 — the fused `writeJSON`/`reconcileBodyLedgerID` doc comments restored, and the three tracer create-status comments corrected (the generated parsers now fill `JSON201`; the facades' any-2xx gate is unchanged, so the accepted status set did not move).
  3. `feat(entities)!` — **alias service deleted.** The server serves no alias route at any version; the resource was renamed to instruments and is v2-only, already covered. Took the `crm` base-URL key, `crmHeaders` and `entities/crm_shared.go` with it. `models/alias.go` became `models/crm_shared_types.go` — the shared CRM types (`BankingDetails`, `RegulatoryFields`, `RelatedParty`) survive because instruments and composition use them.
  4. `feat(entities)!` — **balances (18 methods) and operations (5) migrated onto the generated client**, net −4507 lines. Wire paths preserved exactly; the root-level pipeline test (`legacy_v1_paths_test.go` → `balance_operation_wire_test.go`, still built from `WithBaseURL` against a live server, never an injected base-URL map) went from 6 rows asserting path to 13 rows asserting method + path + query, plus three behavioural guards. Killed the hand-rolled HTTP, `serviceEntity`, `legacyV1BaseURL` and `Entity.baseURLs`.
  5. `refactor(config)!` — the phantom `transaction` service URL removed (see findings).
  6. `feat(entities)!` — **version groups.** `client.V1.<Service>` (14 members) and `client.V2.<Service>` (9 members) for the Ledger plane; the flat ledger accessors are gone. Tracer accessors stay flat. The groups are struct VALUES, not pointers, so a zero-value `Entity` stays guardable — with pointer groups the idiomatic `e != nil && e.V1.Accounts != nil` check panics one level below what the caller can see. `entities/version_groups_test.go` pins both that property and each resource's group membership.
- [x] Epic 2 fix round (2026-08-25, post-review). Two opus reviewers returned FAIL; eleven findings closed plus a spec re-pin.
  1. **CRITICAL, money path** — `fix(entities)!` — a by-id read with an empty id answered **zero, with no error**. The generated client formats ids straight into the URL, so an empty trailing id built `.../balances/`; Fiber runs with StrictRouting unset, trims the slash, and serves the COLLECTION route, whose 200 paginated envelope decoded into a zero-valued `models.Balance`. A reconciliation client reading a balance by an empty id booked `Available: 0` and nothing said the id was missing. Reproduced through the public client against a live server before the fix. The 45 pre-request guards the migration deleted are NOT restored per-service: every facade had the same hole, so one shared guard (`requirePathIDs`) runs at the top of each facade method and returns a typed validation error naming the empty parameter, before anything reaches the wire. Pinned by a table with a row per facade family — 32 rows — each asserting `IsValidationError`, the parameter name, and **zero requests sent**.
  2. `refactor(entities)!` — the alias and external-code balance iterators DELETED. They could not work: the single-page method rejected any non-zero options, so `Limit: 100` produced an error and no request, while the iterator wrote the response cursor back into those same options and had them rejected on the next round. Both single-shot methods also drop their `opts` parameter — a parameter whose only legal value is the zero value is not a parameter. Breaking, accepted for v5 beta.
  3. `test` — the balance/operation wire table claimed to pin "the wire contract"; it pins ROUTING (method, path, query) and its comment now says so, including what it deliberately does not cover. The two migrated iterators that had no cursor-advance guard (account balances, account operations) got one.
  4. `fix(entities)` — a locally refused payload now always classifies as a validation failure. The facades handed back whatever the model's `Validate` returned and the models do not all return the SDK error type, so an empty balance update and a nil operation update reported neither category — the shape a caller is most likely to retry, against a request that never left. One shared helper stamps the category when the model did not; an already-typed error passes through.
  5. `fix(entities)` — the two spellings of the point-in-time account balance read (`V1.Accounts.BalancesAtTimestamp`, `V1.Balances.GetAccountBalancesHistory`) are the same wire call and now enforce the same instant format. The first used to omit an empty date from the query, so the server answered with CURRENT balances while the caller believed they had asked about the past.
  6. `docs` — the shipped godoc stopped teaching the flat accessors the version grouping removed (`midaz.go`, `entities/entity.go`, `entities/iter.go`, `entities/example_test.go`, `pkg/transaction/settlement.go`, `examples/09-testing-with-mocks`), and the two knobs that claimed to reach a legacy transport (`Entity.SetHTTPClient`, `config.WithErrorBodyExposure`) now say they reach no request traffic. `docs/mapping/*` stays Epic 4.
  7. `test(entities)` — the four newly stamped writes (balance create/update/delete, transaction-scoped operation update) got rows in the idempotency-stamp table.
  8. Folded into 6: `entities/entity.go` no longer claims the legacy HTTP client serves balances and operations.
  9. Folded into 3: the wire-test helper's hand-sliced suffix comparison (panics on a short path) replaced with `strings.HasSuffix`.
  10. `test(entities)` — the wiring test walked all nine V2 members but only one V1 member, so a service declared in a group and forgotten in its constructor passed every test and surfaced as a nil dereference on the caller's first call. Both groups are now walked by reflection; mutation-checked by removing one constructor line and watching it fail.
  11. `docs(slices)` — `SLICES.md` still listed 22 source paths deleted by this epic (the hand-rolled services, the alias resource, the transport machinery, `entities/mocks/`) and three paragraphs describing them as awaiting migration.
  12. `chore(api)` — **spec re-pinned to `d816e289b`** (was `07cebd20e`). The v1 `Transaction` response DROPPED `feesSkipped` and `tracerSkipped` (fields and required entries), so `models.Transaction` no longer carries two booleans that could only read false; `TransactionV2` still declares them, which matters when Epic 3 adds a V2 transaction facade. The asset-rate `to` filter gained a description naming its comma-separated serialization. The tracer spec is unchanged at this commit, verified byte-for-byte.
  - Gates green after the round: `go build ./...`, `make test`, `make check-codegen-drift`, `rm -rf ~/.cache/golangci-lint && make lint` (0 issues), `scripts/check-config-parity.sh`.
  - Noticed while re-pinning: `make generate` ran `go mod tidy` and moved `go.uber.org/mock` to indirect — Epic 2 deleted `entities/mocks/` without re-tidying, so `go.mod` had been stale since. Epic 2's gate list should have included `make tidy`.
- [x] Epic 2 verification round (2026-08-25). An adversarial verifier re-checked all eleven fix-round findings against the landed code. Seven confirmed. **The headline one — finding 1, the silent zero — FAILED**: the guard it added rejected emptiness and nothing else, so the same money-path defect was still reachable, and a worse one with it. Two coverage gaps and one doc gap alongside it. The lesson worth keeping is that the round's own criterion (a guard on every by-id read) was met in letter by a guard that did not hold: what was verified was the guard's PRESENCE, never the set of inputs it rejects.
- [x] Epic 2 fix round 2 (2026-08-25, final). Five findings closed, one commit each, plus one commit for a gate failure the round itself introduced:
  1. **CRITICAL, money path** — `fix(entities)!` (`47f9e39`) — the empty-id guard compared the trimmed id against `""` and stopped there, so `"."`, `".."` and `"/"` walked straight through it. The generated client formats ids with the "simple" style, which escapes none of them, and resolves the operation path against the base URL only afterwards — which is exactly where RFC 3986 removes dot segments. Two consequences, both reproduced through the public client against a live server before the fix. **The silent zero was reopened verbatim**: `GetBalance(org, led, ".")` rebuilt `.../balances/`, the collection route answered, and the envelope decoded into a zero-valued `models.Balance` with a nil error. **And destructive scope escalated**, which round 1 never had: `".."` pops a segment, so deleting a ledger issued `DELETE /v1/organizations/{org}/` — deleting the ORGANIZATION — deleting an account targeted the ledger, and deleting an organization reached `DELETE /v1/`. Fixed in the shared helper, so one comparison covers all 125 call sites. Separator rejection was checked against the server before shipping and is safe on every guarded parameter INCLUDING the alias and external-code lookups: Midaz constrains a client-supplied alias to `^[a-zA-Z0-9@:_-]+$` (`pkg/constant/account.go:10`), prohibits the `@external/` prefix outright, and reaches the external account through its own route carrying the bare asset code — so no id the server accepts contains `/`, and no per-parameter exemption was needed.
  2. `test(entities)` (`e350b04`) — the guard was **mutation-invisible on 89 of 122 methods**. The table sampled one call per family, so deleting the guard from `DeleteBalance` left the whole suite green. Replaced sampling with a structural invariant: the test reads the generated request builders to find every operation that formats a value into the URL path, reads the facade functions that name those operations, and asserts each one guards. 132 facade functions over 205 path-parameter operations, and a new facade method is covered when it lands rather than when someone remembers it. Mutation-checked three times; the third mutation exposed a hole in the test itself — the transition helpers take the generated method as a PARAMETER, so their bodies never name the operation and their callers were being credited on an unverified promise. The helpers now get their own assertion.
  3. `test` (`1d9467f`) — validation typing had 4 behavioural rows for 48 sites, the same sampling gap. Covered structurally instead (every `Validate` in `entities` must route through `validationErr`), plus behavioural rows on the writes where a misclassification costs money: transaction create (json/inflow/outflow), transaction update, reserve, billing-package create, billing calculate. Deletes are absent deliberately — they carry no payload, so their only local refusal is the path id, covered in full by finding 2. The 26 list-option sites are exempt because `ValidatePageListOpts`/`ValidateCursorListOpts` build the SDK error type themselves; that exemption got its own behavioural rows rather than a comment asserting it holds.
  4. `docs` (`079dc46`) — the shipped godoc still taught names that do not exist: flat accessors (`c.Organizations.Create`) and renamed iterators (`ListAccountsAll`, `ListAccountTypesPages`). Swept the family rather than the reported sites — ten more `models/*_list_opts.go` headers carried the identical dead spelling. Every name was checked against the live facade surface first, because some `List*All` spellings are real: `ListOperationsAll`, `ListBalancesAll` and `ListAccountBalancesAll` exist and are untouched, as are the already-correct billing-package and fee-package headers, `pkg/transaction/report.go` (struct fields on a counts type, not accessors), and the workflow example's own `ListAccounts` function.
  5. `test(entities)` (`1c30336`) — the unpaired name/value branch got a test pinning that it reports an INTERNAL error, not a validation one: an SDK user cannot fix an unpaired argument list, and calling it bad input would send them auditing their own ids over a defect on our side. `Instruments.List` guards `holderID`, which is a query parameter rather than a path one, and now says why — an empty `holderID` does not fail, it drops out of the query string and widens the request from one holder's instruments to every instrument in the organization.
  6. `test(entities)` (`fc74de3`) — the two new structural scans failed the COLD linter after passing the warm one, which is the cache gotcha this repo has hit before: `parser.ParseDir` is deprecated as of Go 1.25, and its package-then-file grouping (which neither scan used) pushed three functions over the cognitive-complexity limit — one cause, six findings. Replaced with a helper returning the non-test Go files in a directory. Counts identical before and after, and both mutation checks re-run against the refactored scans. Worth remembering for Epics 3-4: run `rm -rf ~/.cache/golangci-lint && make lint` before calling lint green, never the warm one.
  - Gates green after the round: `go build ./...`, `make test`, `make check-codegen-drift`, `rm -rf ~/.cache/golangci-lint && make lint` (0 issues), `scripts/check-config-parity.sh`, `go mod tidy` (no-op).
  - **Residual, accepted — SUPERSEDED, see Epic 3 fix round finding 8, which reversed this and now rejects `%` outright.** The reasoning below was correct and the safety it describes was real; it was also borrowed from the escaping and from the absence of a second decode anywhere downstream, neither of which the SDK can promise. Original note: a percent-encoded dot (`%2e`) is deliberately allowed through — the guard rejects shapes, not strings resembling them. It cannot become a dot segment on our side: the id is escaped once on its way into the path, so `%2e` leaves as `%252e`, and RFC 3986 removal runs on that encoded path and finds nothing. One server-side decode yields the literal text `%2e`. Reaching a traversal from here needs a server that decodes the path TWICE, which would be a defect on that side of the wire. Pinned by a regression row asserting the exact bytes, so any change to the escaping shows up as a diff on that line instead of as a silent traversal.
  - **Method worth carrying into Epics 3-4:** both coverage findings were the same failure — a test that samples examples of a property instead of asserting the property. Where an invariant spans a generated surface, it is cheaper and stricter to derive the expectation from the generated code at test time than to hand-maintain a table. Both new structural tests were mutation-checked, and one of those checks is what caught the unverified-exemption hole in the test itself.
- [x] Epic 3 — **IMPLEMENTED** (2026-08-25), reviewed and fixed (see the fix round below). Six commits, `cd5b1fe`..`02f46e0`, not pushed. Gates green: `go build ./...`, `make test`, `make check-codegen-drift`, `rm -rf ~/.cache/golangci-lint && make lint` (0 issues), `scripts/check-config-parity.sh`, `go mod tidy` (no-op).
  1. `fix(entities)!` (`cd5b1fe`) — **the DELETE 204 defect, fixed once at a shared seam.** All 16 ledger deletes now read the RAW generated response and decide success on the status alone. The same seam also stops an EMPTY error body (a proxy 502, a HEAD-shaped 404) from degrading into "unexpected end of JSON input" — the true status survives. A structural scan (`TestEveryDeleteRoutesThroughTheSharedSeam`) refuses any facade that re-opens a private copy or reaches for the generated `*WithResponse` parser; mutation-checked.
  2. `feat(entities)` (`a8e8705`) — the three generated v1 endpoints with no facade: `transactions/block`, `transactions/unblock`, and the external-account lookup. Block/unblock are CREATES, not transitions (same body as the JSON create, settled immediately, idempotency-stamped). The external account is the counterparty of every deposit and withdrawal and had no reachable route: Midaz prohibits the `@external/` prefix on a client-supplied alias, so the alias route cannot substitute.
  3. `feat(models)` (`d4098c8`) — `TransactionV2`, `OperationV2`, `CreateTransactionV2Input`, `TransactionV2Leg`, `TransactionV2Share`, `UpdateTransactionV2Input`.
  4. `feat(entities)!` (`0a77184`) — **the complete V2 surface.** Thirteen new facades; `client.V2` went from 9 members to 22. Every V2 response is read from the raw generated call, so the v2 surface is immune by construction to all three parser faults (see finding below).
  5. `test(entities)` (`100d9d9`) — the idempotency invariant asserted structurally over 109 facade functions and 122 generated write operations, replacing per-family sampling. Mutation-checked three times.
  6. `style` (`02f46e0`) — cold-linter fixes on the new tests.
- [x] Epic 3 fix round (2026-08-25, post-review). Two opus reviewers: production FAIL (1 HIGH), contrarian correctness PASS (1 MEDIUM, 5 LOWs). Both found the same central defect independently. Nine commits, `49f7601`..`25a124a`, not pushed.
  1. **HIGH, money path** — `fix(entities)!` (`49f7601`) — **a 2xx carrying no resource read as a zero-valued object with a NIL error.** A create that answered 201 with a body of `null`, `{}`, or nothing handed back a transaction with id `""` and status `""` and no error: `json.Unmarshal` on `null` is a documented no-op and `{}` sets nothing. A caller branching on `err != nil` booked a settled transfer that carried no identity. Fixed **once, in the shared single-object decode path**, so it covers every read and every write on both surfaces rather than the transactions where the reviews found it. It reports a **response-decode** error, which is the honest class — the server answered, so on a write the operation may already have taken effect and `errors.IsResponseDecodeError` already documents that as "outcome unknown". Safe against false refusal, checked rather than assumed: the ledger OAS has **no** single-object endpoint answering 2xx with no body (the only bodiless 2xx responses are the 27 DELETE 204s and the 14 HEAD counts, neither of which reaches this path), and the one id-less single-object read — `LedgerSettings` — marks `accounting`, `tracer` and `overrides` all required, so `{}` is invalid there too. Pinned on one create, one lifecycle transition and one plain read on BOTH surfaces × four body shapes (empty, whitespace, `null`, `{}`), plus the positive half.
     - **`Cancel`'s exemption was re-derived, because its stated reason was wrong.** The old comment said the server's /v2 projection is nil-preserving. It is (`transaction_v2_output.go:232`), but it is SHARED by the creates, commit, cancel, revert, get and update alike and its nil branch is unreachable from any of their success paths — so it singles cancel out from nothing, and the contrarian was right that the same argument would exempt commit and revert. What actually justifies it is narrower: **cancel's answer is fully determined by the request** — the transaction the caller named is CANCELED and the caller supplied its id, so synthesizing invents nothing. Revert answers with a NEW CHILD transaction whose id the caller cannot know; commit answers with the settled state the caller called to observe. Both now fail loudly.
     - `V1.Transactions.Get` moved off the generated parser onto the raw path its siblings already use — it was the only read in the money-path facade still going through it, and an empty body failed INSIDE the parser as an internal error instead of reaching the guard.
  2. **Extension beyond both reviews** — `fix(entities)!` (`cbd0a8d`) — the same defect on the LIST path, which on a reconciliation is the worse one: `null`/`{}` decoded into a zero-valued envelope and returned an **empty page with no next cursor** and a nil error, so a caller walking a ledger's transactions stopped on the first dropped body and concluded the ledger was empty. Reproduced before fixing. Same evidence justifies it — `Pagination` and `TransactionV2ListBody` both mark `items` and `limit` required, so an empty page is `{"items":[],"limit":N}`. **The bare-ARRAY reads are deliberately exempt**: Go's `encoding/json` marshals a nil slice as the literal `null`, so on that shape `null` is what a handler with no results emits, and the object guard would turn "no balances at that instant" into an error.
  3. **MEDIUM** — `test(entities)` (`1db7aaa`) — **the delete-seam scan was defeatable.** The contrarian wrote a compiling facade that called the banned generated parser and passed every check. Three escapes, all from asserting the SHAPE of the call: a client reached through a receiver field spelled anything but `ledger`/`tracer`; an operation passed as a function VALUE rather than called; a client hoisted into a local. It now reads the DELETE operations out of the generated clients and matches any selector naming one — the approach the sibling idempotency scan already used. All three bypass shapes were re-introduced and confirmed to fail before the probe was removed. The two tracer deletes that predate the seam are named explicitly with their reason and checked against the promise they are credited for, replacing a blanket allowance for `readRawResponse` any facade could claim. Floor raised 14 → **29** (27 ledger + 2 tracer).
  4. **MEDIUM** — `fix(entities)!` (`bd71d63`) — **the /v2 transaction list sent six filters the ledger ignores.** Setting `Filters.Status` returned EVERY transaction in the ledger with a nil error. Now refused before the request is built, naming every filter the caller set — the position `V2.Operations.ListOperations` already argued for its own. Count is untouched: `countTransactionsByFiltersV2` DOES declare status and route. **The briefing's premise for exempting /v1 was wrong and this is the one deviation in the round:** it said "/v1 declares them". It does not. ~~`getAllTransactions` and `getAllTransactionsV2` declare **byte-identical** query sets (`metadata, limit, start_date, end_date, sort_order, cursor`), verified twice against the pinned spec~~ — **corrected in fix round 2: that was the SDK's pinned spec; the server's own OAS declares none of the six for either operation, and the two routes are the same server function. See fix round 2, finding 3.** The SDK's own `TransactionsFilters` doc already said the filters are ignored "on either endpoint". So narrowing on /v1 is just as silent. ~~/v1 was left sending them because that is how the surface shipped and closing it changes a shipped contract — a product decision, not an implementation one, and it is open.~~ **Closed in fix round 2: /v1 refuses them too.**
  5. LOW — `fix(entities)` (`5a72912`) — a leg naming the ADDRESSED ledger with a stray space was refused as a leg naming a DIFFERENT one: the emptiness test trimmed and the comparison did not. Both ignore whitespace now, and the leg carries the trimmed value so the accepted spelling and the spelling the ledger reads are the same string. The trim writes into the scoped copy, so an input reused across two ledgers still cannot carry the first call's normalization into the second.
  6. LOW — `fix(models)` (`ee0305a`), four divergences in one commit: **(a)** a padded money string passed validation and travelled verbatim to a server 400 — validation now judges the exact bytes that leave, since the SDK does not rewrite money text; **(b)** a v2 leg alias containing `#` was accepted locally and refused by the ledger, and the reason is a LOST ENTRY, not formatting — the ledger keys per-entry maps on a composite `index#alias#balanceKey` and leaves an already-composite-looking alias unmutated, so a forged one collides or matches nothing and the transaction moves value in one direction only (`/` stays legal: `@external/<ASSET>` is the only way to spell funding on a surface with no inflow/outflow action); **(c)** a >256-char description was refused on /v1 and accepted on /v2, now shared; **(d)** the /v2 update doc claimed /v1 accepts an `externalId` there — it does not, both surfaces reference the same request schema and /v1's `ExternalID` is `json:"-"`, so the two types are wire-identical.
     - **The "required description AND metadata" concern was chased to the handler and is a schema artifact.** Both update routes register `SkipValidateBody` and read a raw body (`transaction_routes.go:142`, `transaction_v2_mirror_register.go:60`), then decode imperatively into a struct whose only validators are `max=256` on the description and per-entry bounds on the metadata — **no required tag on either field** (`postgres/transaction/transaction.go:90`). Huma's own note says the typed body is documentation-only and never touches the request path. A description-only patch is accepted; recorded in the type's doc comment with those references.
  7. LOW — `test(entities)` (`9c79e23`) — the one-spelling-per-endpoint guard listed the METHOD NAMES that must not come back, which a contributor walks past by choosing another name — and the name is exactly what someone changes when a test complains. It now counts the V2 facades reaching each generated operation and requires exactly one; a generated operation is one method and one path, so that is the property the surface wants. Re-added under a new name, the account-scoped balance read was confirmed to fail before the probe was removed. 83 V2 operations covered.
  8. **The percent-encoding residual was REOPENED and closed the other way** — `fix(entities)!` (`25a124a`). Last round accepted `%2e%2e` as harmless: the client escapes once, `%2e` leaves as `%252e`, RFC 3986 removal finds nothing, Fiber 404s. All true — and **borrowed**. It holds only while the client escapes exactly once and nothing between here and the ledger decodes again, neither of which this SDK can promise; a gateway normalizing a path before forwarding turns the 404 back into the scope-escalated DELETE the guard exists to prevent, silently. What settles it is that refusing costs **nothing**: no value Midaz can serve from a path segment contains `%`, because none can have been CREATED with one — every UUID param is `uuid.Parse`d, `entity_name` is a closed enum, an alias is constrained to `^[a-zA-Z0-9@:_-]+$` at account creation (`pkg/constant/account.go:10`), a metadata index key to `^[a-zA-Z][a-zA-Z0-9_]*$` at index creation, and asset/external codes are tickers. Three of those lookups do not re-validate the path segment, so the server would accept a `%` and match nothing — an absence of validation, not an allowance. The old regression row asserting the escaped bytes is replaced by four rejection rows plus a positive row proving the guard still passes the characters a real alias is made of.
  9. `style` (`fad956f`) — cold-linter fixes: a table closure taking `*testing.T` reads as an unmarked helper, and an AST scan inlined into its test function breached cognitive complexity. Same two shapes this repo hit in Epic 2 round 2. **The warm linter passed both times.**
  - Gates green after the round: `go build ./...`, `make test`, `make check-codegen-drift`, `rm -rf ~/.cache/golangci-lint && make lint` (0 issues), `scripts/check-config-parity.sh`, `go mod tidy` (no-op).
  - ~~**Open, needs a product decision:** whether `V1.Transactions.List` should refuse the six undeclared filters as /v2 now does.~~ **Closed in fix round 2, finding 3.**
  - **Method note.** Two of the three review findings that mattered were tests that asserted a property's SHAPE rather than the property (the delete seam matched on how a call was spelled; the one-spelling guard matched on method names). Epic 2 round 2 recorded the same lesson about sampling. The generalization worth carrying: derive the expectation from the generated code, and match on the OPERATION, never on the syntax around it.
- [x] Epic 3 fix round **2** (2026-08-25, post-verification). Adversarial verification of round 1 returned FAIL: 2 HIGHs — **both inside the round-1 fixes themselves** — 1 MEDIUM, 3 LOWs, plus one settled fact that closed the open product decision. Seven commits, `fb37d69`..`86a16a4`, not pushed.
  1. **HIGH, money path** — `fix(entities)!` (`fb37d69`) — **round 1 fixed the empty-list-body defect on ONE surface.** The guard lived in the v2 helper; all twenty-six v1 lists still decoded their envelope inline, so on /v1 a 2xx carrying `null`, `{}` or nothing was still an EMPTY PAGE with an empty next cursor and a nil error — a caller walking a ledger still concluded it was empty. Reproduced on transactions, accounts and balances before fixing. **Every list on both surfaces now reads the raw response through `readList`**, which is what puts the empty shape in front of the guard at all: the generated `*WithResponse` parser unmarshals the body itself whenever the content type says json, so a bodiless 200 failed INSIDE it and surfaced as an internal error before any facade guard could run. That is why routing the inline decodes through a shared decoder was not enough on its own — the first attempt did exactly that and `null`/`{}` passed while empty/whitespace still did not. The four tracer-plane lists decode a named-field envelope of their own (`{"rules":[...],"nextCursor":""}`) and cannot take `readList`; they call the extracted `guardListBody`, the same pair of refusals without the shared envelope type. **Secondary defect closed at the same seam:** a malformed or empty 2xx list body reported `NewInternalError` ("the SDK is broken") instead of `NewResponseDecodeError` ("the server answered and the answer is unreadable"), which is the fact a caller needs in order to decide whether to reconcile; four tracer tests pinned the old class and were updated with that reason. Net **−160 lines**, twenty-six copies of one block deleted. Pinned on transactions, accounts and balances × the five no-page body shapes, plus the positive half on each. Side effect worth recording for Epic 4: the `*WithResponse` retrofit inventory drops from 82 sites to **48**, and every list is now off the generated parser.
  2. **HIGH** — `test(entities)` (`c14520d`) — **round 1's own delete-seam hardening was still defeatable, and so was its sibling.** Both structural scans iterated `file.Decls` and skipped everything that was not a function with a body, so a generated operation bound at PACKAGE scope was invisible to both — and the facade calling it then named a bare identifier carrying no operation name, leaving nothing to check. Two compiling shapes were written against the real generated client — a package-level `var` holding the method value, and a package-level map of closures returning it — and confirmed to leave **both** `TestEveryDeleteRoutesThroughTheSharedSeam` **and** `TestEveryPathParameterOperationIsGuarded` green. Verified in both directions: with the escapes in place the pre-fix scans passed and the post-fix scans failed on all four combinations; the probe was then deleted and the two shapes recorded in the test. Reflection stays out of AST reach for either scan, and the tests now say so rather than implying coverage they do not have.
  3. **The open product decision is CLOSED: /v1 refuses the six dead filters too** — `fix(entities)!` (`55eab5e`). Round 1 left this open on the premise that /v1 "shipped sending them", and recorded the supporting claim as *"getAllTransactions and getAllTransactionsV2 declare byte-identical query sets"*. **That claim is corrected here: it described the SDK's pinned spec, and the server's own OAS declares none of the six for either operation.** The fact is settled at the handler, not at any spec: **both list routes call the SAME server function** — `transaction_handler.go:500` and `transaction_v2_mirror_handler.go:148` both invoke `handler.getAllTransactions` with the raw query values. `status` and `asset_code` are parsed and then DISCARDED (`ToCursorPagination`, `pkg/net/http/httputils.go:533-539`, returns only limit/cursor/sort_order/start_date/end_date, and that struct is the only value the repository receives — `transaction.postgresql.go:1333`); `reference`, `source_account`, `destination_account` and `route` are never parsed at all (no case for them in the switch at `httputils.go:150-252`). So the filters NEVER worked on either surface: a caller setting `Filters.Status` received the full unfiltered set and had no way to know. **Orchestrator decision:** v5 is already a breaking major and this removes the surface's last silent full scan, so the six request editors are deleted and the filters refused before the request is built. `Pages`/`All` inherit; `Count` is untouched because it genuinely declares and honours status and route. The handler evidence is cited in the facade doc so the next reader does not re-litigate it, and the refusal tests now run over both surfaces from one table — including the positive half proving /v1 kept its cursor/limit/sort/date params and that the six dead names no longer reach the wire.
  4. **MEDIUM** — `docs(entities)` (`f14252b`) — **`Count`'s default window is TODAY, not the ledger.** The server defaults an unset range to the current UTC day (`count_transactions_by_filters.go:63-65` and `75-77`) and the SDK omits the dates when they are unset, so the default `Count` answers "how many transactions today" — while both `Count` docs, the `TransactionsFilters` docs and the list refusal message all steered callers to `Count` as the way to narrow by status or route without saying which window they would get. The number looks plausible, which is what made it worth writing down. **Documentation only:** `Count` is deliberately NOT made to require a range — that is a heavier contract change with no defect behind it.
  5. LOW — `fix(entities)` (`c4708a4`) — a whitespace-only filter value slipped BOTH nets: the refusal trimmed before testing, so it was not named, and no editor emitted it either, leaving the same silent full result set reached through a nonsense input. A non-empty pre-trim value now counts as set; emptiness means the zero value.
  6. LOW — `docs(entities)` + test (`9a4f6b4`), two in one commit: **(a)** the synthesized `Cancel` result carries ONLY the id and the CANCELED status — amount, operations, metadata and every timestamp come back zero with a nil error — and neither doc said so; both now do, and both record that the branch is unreachable against the pinned server (`/v1` and `/v2` cancel are shells over one `commitTransaction` core, `transaction_handler.go:293-298` and `transaction_handler_v2.go:208-213`, and both always project a populated body), so the tolerance reads as gateway insurance rather than as documented server behaviour. **(b)** the empty-body corpus gains a `"null\n"` row: most JSON writers append the newline, and `TrimSpace` was pinned by exactly one case, which left every non-Cancel endpoint free to lose the trim unnoticed.
  7. `style` (`86a16a4`) — cold-linter fixes. **The same two shapes as round 1, for the third round running:** a table closure taking `*testing.T` reads as an unmarked helper (`thelper`), and a surface-by-shape loop nested inside its test function breaches cognitive complexity (`revive`). The warm linter passed both times again.
  - Gates green after the round: `go build ./...`, `make test`, `make check-codegen-drift`, `rm -rf ~/.cache/golangci-lint && make lint` (0 issues), `scripts/check-config-parity.sh`, `go mod tidy` (no-op).
  - **Method note, and it is the finding of this round.** Both HIGHs were in round 1's own fixes, and both failed the same way: **a fix applied at the surface where the defect was FOUND rather than at the boundary where the property must hold.** The empty-body guard went into the helper the reviewer's example used, leaving twenty-six siblings uncovered; the seam scan was hardened against the three escapes a reviewer wrote, leaving package scope entirely unwalked. Neither was a wrong fix — both were correct and both were incomplete, and the incompleteness was invisible because the test proving each one sampled the same surface the fix touched. The rule worth carrying into Epic 4: **when a fix is central, prove it from a site the fix did NOT touch**, and enumerate the boundary rather than the examples.
  - ~~**Residual, not changed:** `TransactionsListOpts.ToQueryParams` still renders the six refused filters into its wire map.~~ **Closed in fix round 3, finding 4a: deleted.**

- [x] Epic 3 fix round **3** (2026-08-25, final). Adversarial verification of round 2 returned FAIL: 2 HIGHs, 3 MEDIUMs, 2 LOWs. Five commits, `bb81ec6`..`0443dd5`, not pushed. Two MEDIUMs deferred to Epic 4 by orchestrator decision (recorded below and folded into the Epic 4 inputs).
  1. **HIGH** — `test(entities)` (`bb81ec6`) — **round 2's list guard was correct on the tracer plane and completely unpinned there.** The verifier reverted all four tracer facades to a bare non-2xx check and the ENTIRE suite stayed green: the silent-empty-page defect live on every tracer list, with CI passing. The cause is the same shape as round 2's own finding — the pin table carried rows only for the surfaces the round-2 fix had used as examples (three ledger v1 + one ledger v2), and the tracer lists do not route through `readList` at all; each decodes its own domain-keyed envelope and calls `guardListBody` directly, so no ledger row can speak for them. **Four rows added, one per envelope** (`rules`, `limits`, `transactionValidations`, `auditEvents`), each carrying that endpoint's own real-empty-page body so the positive half exercises the actual envelope rather than a ledger-shaped stand-in. **Verified by re-running the verifier's mutation:** with the four tracer arms reverted the suite now fails 12 subtests across all four rows (the `null`, `null\n` and `{}` shapes — the empty and whitespace bodies still fail inside `json.Unmarshal`, which is why they never caught it), and every ledger row stays green, confirming the tracer rows are what carry the pin. Mutation reverted, suite green.
  2. **HIGH, product surface** — `fix(entities)` (`ce8f66b`) — **`Count` could not take a date range on either surface while three doc sites promised it could.** Measured both ways: an RFC3339 `StartDate` — the format the count endpoint actually parses (`count_transactions_by_filters.go:57` and `69`) — is refused by the SDK validator, because the opts struct is shared with `List` and its validator enforces the ledger list format; a `YYYY-MM-DD` value passes the SDK and comes back from the server as a 400 `ErrInvalidDatetimeFormat`. **No spelling worked.** Meanwhile round 2's own documentation commit (`f14252b`) told callers in three places to "set StartDate and EndDate to count any other span", and the round-2 refusal of the six dead list filters steers callers to `Count` as the remaining way to narrow. So the one narrowing the surface still offered was unreachable. **Orchestrator decision — the caller keeps ONE date format, `YYYY-MM-DD`, for both `List` and `Count`.** `Count` widens each named day at param-mapping time to the instants the server itself defaults to when the dates are absent (`00:00:00Z` for the start, `23:59:59.999999999Z` for the end, `count_transactions_by_filters.go:63-65` and `75-77`). **End-of-day on the upper bound is the load-bearing half:** the count query compares `created_at <= end_date` (`transaction.postgresql.go:1594-1595`, both bounds inclusive), so a midnight bound would silently drop every transaction on the last day the caller named. The validator was deliberately NOT switched to RFC3339 — that would make the same two fields mean different things depending on which method reads them, which is the defect's root. `ValidateCursorListOptsRFC3339Dates` (the tracer plane's) is untouched. No breaking risk: nothing worked before, so nothing depended on it. One mapper serves both surfaces (`countTransactionsV2Params` delegates), so the fix lands once. Pinned on both surfaces × three properties (a range reaches the wire as the RFC3339 pair and both bounds re-parse as RFC3339; an unset range is still omitted so the server's today-default applies; a malformed day is still refused locally). **Both mutations caught** — a midnight upper bound and no widening at all — after the first draft of the pin was itself found circular, asserting against the same constants the code interpolates; the assertions are literals now.
  3. **MEDIUM** — `fix(entities)` (`3b64ad8`) — **three v1 array reads were the last sites still on the banned generated parser.** `MetadataIndexes.List`, `Balances.GetAccountBalancesHistory` and `Accounts.BalancesAtTimestamp`; their v2 twins already went through `readSlice`, so the same endpoint answered differently depending on which surface reached it. The parser unmarshals the body itself whenever the content type says json, so it failed BEFORE any facade logic ran: an unreadable 2xx became `internal_error`, and — the expensive one — **a gateway 403 or 404 carrying an empty body became `internal_error`/500**, destroying the status the caller needs to choose between "you are not allowed" and "it is not there". All three now read the raw response. Pinned from a table over all four bare-array reads (the three fixed sites plus the v2 twin), verified against the pre-fix sources: the three v1 rows fail on both properties, the v2 row stays green.
  4. LOWs, two commits: **(a)** `refactor(models)!` (`5a3bd6e`) — `TransactionsListOpts.ToQueryParams` deleted. No caller outside its own tests, and it rendered all six filters the round-2 fix now refuses, so the only thing it could still do was tell a reader those filters narrow something. The tests asserting the six render went with it; what the list can still express is pinned against both live surfaces in the entity tests, and the metadata pair's validation rules stayed. **(b)** `docs(entities)` (`0443dd5`) — the response-decode rationale round 2 inserted into four tracer tests sat ABOVE the `// TestXxx` line, so the doc no longer opened with the function name and godoc rendered it as the summary of the preceding test. Moved below the opening line in all four.
  - Gates green after the round: `go build ./...`, `make test`, `make check-codegen-drift`, `rm -rf ~/.cache/golangci-lint && make lint` (0 issues), `scripts/check-config-parity.sh`, `go mod tidy` (no-op).
  - **Method note.** Round 2's rule — *when a fix is central, prove it from a site the fix did NOT touch* — was right and was applied too narrowly: round 2 proved its guard from sites it had not touched **on the ledger plane**, and the tracer plane, which needed a different pin because it decodes differently, got none. The sharper form: **enumerate the DECODE PATHS the property must hold on, not the endpoints.** Four tracer envelopes are four paths; twenty-six ledger lists sharing `readList` are one. Both HIGHs in this round were also found by mutation rather than by reading, which is the second thing worth carrying: a green suite proves nothing about a guard until the guard has been removed and the suite has gone red.
- [x] Epic 3 verification round 3 + orchestrator close-out (2026-08-25). A bounded adversarial verifier re-checked ONLY the four round-3 fixes, by mutation. **Fixes 1, 2 and 4 verified clean:** the tracer-arm mutation reproduced exactly 12 red subtests with every ledger row green; the Count widening was re-derived independently from the server source (RFC3339 parse at `count_transactions_by_filters.go:57,69`; both bounds inclusive at `transaction.postgresql.go:1594-1595`); the Count pin is genuinely literal and caught a THIRD mutation (bounds swapped) beyond the two the round claimed; the wire carries the date pair exactly once, on Count only, with List unwidened; `ToQueryParams` has zero remaining callers. **Fix 3 FAILED on enumeration: `readSlice` has FIVE call sites, not four.** `metadata_indexes_v2_facade.go` (`V2.MetadataIndexes.List`) had no row in `bareArrayReads`, and mutating it back to the banned parser left the ENTIRE suite green — the exact defect the fix claims to have eliminated, alive on the path the table missed. The table's own doc comment ("on BOTH surfaces") already promised the missing row: the signature of the mid-round `git checkout` loss the round-3 implementer reported, where the prose survived and the row did not. **Orchestrator fix:** fifth row added; mutation re-run — the new row alone goes red on all three properties (403 stays authorization, 404 stays not-found, unreadable 2xx is a decode error) and every other row stays green. Two LOWs fixed alongside: the `listReads` fixture comment overstated what the per-row bodies prove (json.Unmarshal ignores unknown fields, so the positive half proves the guard ACCEPTS a real empty page, not that the envelope decodes — comment corrected; envelope decoding is pinned by each facade's own tests) and `docs/mapping/external_apis.md` still claimed every opts struct exposes the deleted `ToQueryParams` (a blanket claim already wrong for three others — corrected to name the four structs that map straight onto generated params). Informational, deferred to the Epic 4 docs pass: `docs/plans/transaction-metadata-list-filter.md` is stale since the v4 remodel (predates this epic).
  - Gates green after the fix: `go build ./...`, `make test`, `rm -rf ~/.cache/golangci-lint && make lint` (0 issues).
  - **Method note, fourth occurrence of the same failure shape.** Round 3 stated the sharper rule — enumerate the DECODE PATHS, not the endpoints — and violated it within the same round by COUNTING four `readSlice` sites where five exist. Counting is not enumerating; the only enumeration that holds is one a test enforces. Epic 4's "no facade calls a generated `*WithResponse`" scan is the closing move for this class: it makes the enumeration mechanical instead of remembered.
- [x] **Epic 3 — CLOSED** (2026-08-25, orchestrator final review). Full V2 surface live behind `client.V2` (22 services), the v1 repairs landed, and the empty-body/status-preservation guards pinned on every decode path: `readOne`/`decodeOne`, `readList` (26 ledger lists), the four tracer envelopes via `guardListBody`, and all five bare-array `readSlice` sites. 28 commits total across implementation + three fix rounds + close-out, `cd5b1fe`..HEAD, not pushed. Deferred to Epic 4 and recorded there: the 45-site `*WithResponse` retrofit (now correctness, not tidiness), the vacuous-guard hardening of the path-id scan, and the docs debt inventory.
- [x] **Epic 4 phase A — generated-parser retrofit + structural hardening** (2026-08-25). Six commits, `8e4d7a3`..`f48b79e`, not pushed. Closes both of Epic 3's structural deferrals; the docs debt inventory is untouched and stays open for phase B.
  1. **The scan was written FIRST, and it is the enumeration** — `test(entities)` (`ab4f2fe`) — `TestNoFacadeCallsAGeneratedParser`. It parses every non-test file in `entities/`, matches every selector against the operations read out of both generated clients, and refuses any facade naming a `*WithResponse` spelling. Run against the pre-retrofit tree it reported **exactly 45 sites, confirming the plan's number** — which had been reached by counting and was therefore the one number in this epic most likely to be wrong. It is now 0. Three things were deliberately built in, each from a scan in this package that a reviewer or a mutation already walked past: it inspects the WHOLE file rather than `FuncDecl`s, so a package-scope binding cannot hide; it matches only the OPERATION NAME, never the shape of the call (not the receiver's field name, not whether the selector is the callee); and it does not assert emptiness alone — an emptiness assertion passes trivially once the matching breaks, so a floor on the raw call sites (225 distinct operations today, 181 before) fails if the scan goes blind. **No exemption list, because no site needed one**; the doc comment says a future exemption belongs there with its reason rather than as a loosened match.
  2. **The retrofit, 45 sites in four commits** — `refactor(entities)` (`8e4d7a3`, `864daa0`, `be36966`, `85300ae`) — the v1 CRUD families (24), balances and operations (6), the remaining v1 ledger facades (9), and the tracer plane (6). Each block SHORTENS: `readOne` over the raw call for reads, `readRawResponse` inside the existing `writeJSON` closure for writes. **Behaviour bought, on a money-path surface:** a gateway 403 or 404 answering with an EMPTY body keeps its real status instead of failing inside the parser's unmarshal and arriving as `internal_error`/500 — the caller can again tell "you are not allowed" from "it is not there"; an unreadable 2xx is a response-decode error ("the server answered, the answer is unreadable", so a write may have taken effect) rather than an SDK-internal fault; and a non-UUID id no longer reports as an SDK bug. Wire behaviour is unchanged throughout — same operations, paths, query parameters and idempotency editors. **Correction (fix round, finding 6): the claim "zero behaviour change beyond the intended one" was FALSE, by one.** The retrofit routes all 45 sites through `readRawResponse`, which caps the body at 10 MiB (`maxHTTPResponseBodyBytes`); the generated parser did an unbounded `io.ReadAll`. So a caller whose single-object response exceeds 10 MiB now gets an error where it previously got an unbounded read. The cap is desirable and is the same one the money path has always applied — this aligns CRUD with it rather than introducing something new — but it is a fourth behaviour change and the phase did not name it. No code change; recorded because an unenumerated behaviour change is exactly the shape this epic keeps being bitten by. **One split closed inside a single endpoint:** the transaction-scoped operation update is spelled twice on /v1, and `V1.Transactions` already read it raw while `V1.Operations` read it through the parser, so the same call answered differently depending on the spelling. That overlap is why 181 + 45 raw operations is 225 and not 226.
  3. **Pinned by sample + scan, which is the closing move** — `test(entities)` (`ab4f2fe`) — a seven-row behavioural table asserting the three properties (403 stays authorization through an empty body, 404 stays not-found, an unreadable 2xx `IsResponseDecodeError`) plus a positive half asserting the decoded id actually lands. The rows span the SHAPES the one decode path is reached through, not the resources returned: a plain retrofitted read, a money-path read, the tracer plane's separate client, both SURFACES of a dual family (shown to agree, which they did not before), and the two v1 lifecycle transitions — **which the retrofit did not touch**, applying round 2's rule that a central fix is proven from a site the fix did not edit. **Correction (fix round, finding 7): the row described here and in `ab4f2fe`'s message as "the dual spelling" was the dual FAMILY (V1/V2 `Accounts.Get` — two endpoints on two surfaces), not the dual SPELLING the phase claimed to have closed (`V1.Transactions.UpdateOperation` vs `V1.Operations.UpdateTransactionOperation` — one generated operation, two facade names, which answered differently until the retrofit). The real pair is now two rows, so the claim is true instead of reworded; they also add the one shape the table lacked, a WRITE reaching the shared decode through `writeJSON` rather than `readOne`. The doc comment states the reasoning the epic arrived at the hard way: a behavioural row per site would be forty-five servers proving one helper forty-five times, so the table does not try to BE the enumeration — the scan is, and sample plus scan is what covers every site. Each row carries its OWN success body, because the tracer's `Limit` keys identity on `limitId` where every ledger resource uses `id`, and a shared ledger-shaped stand-in would have decoded a zero id and proved nothing.
  4. **MEDIUM-2 recorded CLOSED here — and the closure was FALSE. Repaired in the fix round below (`5f3a45e`); read that entry with this one.** The identity check `88b0503` added was necessary and is intact, but it left the scan checking the right NAMES rather than checking that the guard guards: five compiling facades sent `id=".."` to the wire with a nil error and passed it, and one of them was the **vacuous guard this very item claims to have closed**, reached one hoisted local away. The claim below is therefore true of what the commit did and false about what it achieved; both halves are kept because a false closure discovered and repaired is worth more on the record than a tidy line. Original text — `test(entities)` (`88b0503`) — `TestEveryPathParameterOperationIsGuarded` accepted `requirePathIDs(op)` naming no pairs, which returns nil on an empty list. It now compares expressions: the path argument POSITIONS are derived from the generated code (the request builder names the parameters it styles into the URL, the client method's signature says where they sit in the argument list), so every value a facade forwards into a path segment must be one it handed to the guard. The three transition helpers got the same treatment instead of keeping the weaker "calls requirePathIDs somewhere" test — they are the more expensive place to leave the hole, since thirty-odd facade methods are credited purely on their promise. **Zero live instances tripped**, as the deferral predicted, and the credited-function count is unchanged at **209 across 205 path-parameter operations, measured on both versions**, so the stronger criterion covers the same surface rather than a smaller one.
  5. `style(entities)` (`f48b79e`) — cold-linter fix, cognitive complexity 22 against a limit of 20 on the rewritten scan. **The same shape for the fourth round running** (a surface-by-shape loop nested inside its own test function), and **the warm linter passed it again**. Per-declaration classification moved onto a small struct method.
  - **Mutation results, all four run and reverted; the two on the path-id scan re-run AFTER the lint refactor rather than trusting the pre-refactor proof:**
    1. Reverting one retrofitted site the pin table does NOT sample (`V1.Segments.Get`) turns the scan RED, naming `segments_facade.go:126`, while the behavioural table stays GREEN — the table cannot see segments, only the scan can, which is the demonstration that neither half is sufficient alone.
    2. Breaking the status preservation inside the shared decode (`DecodeProblemJSON` → `NewInternalError`) turns **14 table subtests** RED (7 rows × 2 statuses) while the scan stays GREEN — the scan cannot see behaviour.
    3. Stripping the pairs from `Accounts.Get`, a money-path by-id read, leaves the **OLD** scan and the ENTIRE package green, and the new scan names all three forwarded arguments. The hole was real, not theoretical.
    4. Stripping the pairs from `ruleTransition` fails the helper assertion.
  - **Commit-order note.** Every commit in the series is green: `go build` + the `entities` suite were verified at each of the four retrofit commits by checkout. The scan was written and run first — that is how the 45 was established — but it lands in the fifth commit rather than the first, because committing a knowingly-red test into a money-path repo poisons `git bisect` for anyone who lands in the middle of the series. The red measurement is recorded here and in that commit's body instead.
  - Gates green, in order: `go build ./...`, `make test`, `make check-codegen-drift`, `rm -rf ~/.cache/golangci-lint && make lint` (0 issues), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`).
  - **Method note.** The four-round failure this phase was built to end was never a shortage of care — each round's enumeration was done attentively and each was wrong by one. What changed here is only the ORDER: the scan was written before the retrofit, so the site list was produced by parsing rather than by reading, and the number the plan carried was confirmed by a tool instead of re-derived by a human who already believed it. The generalisation for the rest of Epic 4: when a phase's brief states a count, the first artefact built should be the thing that can measure it.

- [x] **Epic 4 phase A fix round** (2026-08-25, post-review). Two opus reviewers, production and contrarian, both returned FAIL. **Every finding was in the ENFORCEMENT artefacts — the structural scans — and none in the 45-site retrofit**, which both verified clean by exhaustive diff. Six commits, `5f3a45e`..`3f59eee`, not pushed. The docs debt inventory is still untouched and stays open for phase B.
  1. **HIGH** — `test(entities)` (`5f3a45e`) — **the path-id scan checked that the right NAMES appeared in the function, not that the guard guards.** `guardedValues` collected `requirePathIDs` arguments from the whole body as source text: no ordering, no reachability, no scope resolution. Five compiling facades sent `id=".."` to the wire with `err=nil` and passed: the guard placed AFTER the generated call; the guard nested inside an `if`; a guarded variable reassigned before the call; the **vacuous `requirePathIDs(operation)` with the call hoisted to a local** — the hoist made `call.Fun` an `*ast.Ident`, the direct-call count fell to zero and classification dropped to the presence-only branch, **which is exactly the hole `88b0503` claimed to have closed**; and an inner scope shadowing a guarded name. Closed for the first four by requiring the guard to sit at statement depth 1, run unconditionally within its statement, and appear positionally before the statement carrying the call — plus following a hoisted binding in `directPathCalls`, which is the closure the sibling delete-seam scan already had and this one did not. The presence-only branch now additionally requires real guarded pairs, so the vacuous guard cannot be reached through the hoist either. **The last two are NOT closed and are documented as a known ceiling** next to the reflection one: a reassignment between guard and call, and a same-name shadow, are invisible to source-text comparison and need type-checked SSA to close honestly. **One accepted false positive is documented as deliberate:** guarding `orgID` and forwarding `strings.ToLower(orgID)` is still reported, because the only way to accept it is to loosen the match until an expression MENTIONING a guarded name counts — and that same loosening accepts `orgID + "/../"`. **Proof, by re-running every escape:** the four closed shapes are flagged, the two ceiling shapes are not, and a hoist with a COMPLETE guard is not (the false-positive control for the new binding-follower). Against the PRE-fix scan the same probe file reported only the false positive — all five real escapes passed. Credited functions unchanged at 209 across 205 path-parameter operations, so the stricter criterion covers the same surface rather than a smaller one.
  2. **HIGH, found independently by both reviewers** — `test(entities)` (`20fd2a3`) — **the parser scan banned one of the parser's TWO spellings.** It refused the `*WithResponse` METHOD, while the generated package exports the same parser as free functions beside it — `Parse<Op>Resp`, 197 in genledger and 28 in gentracer. `GetSegmentByIDWithResponse` **is** the raw call followed by `ParseGetSegmentByIDResp`, so a facade doing those two steps by hand reproduced the banned behaviour byte for byte and the scan reported zero offenders. `generatedOperation` now recognises the free-function spelling as the fifth name oapi-codegen emits per operation, and the suffix test became a shared `isParserSpelling` so the delete-seam scan moves with it. The Parse/Resp strip is unambiguous: no generated operation is itself named `Parse*`, checked against both clients' request builders. **Proof:** the probe facade is flagged by the new scan (`names ParseGetSegmentByIDResp`) and passed the pre-fix scan. Still 0 offenders and 225 raw operations on the real tree — no live site used it.
  3. **MEDIUM** — `test(entities)` (`c7613be`) — **the scans' scope was a hardcoded ".", asserted nowhere.** A helper in another package taking the generated client passed all three. `entities/` is TODAY the module's only importer of `internal/genledger` and `internal/gentracer`, so widening every scan buys nothing; the cheaper and honest half is a tripwire, and it is now asserted rather than assumed. Imports are PARSED, not grepped — the text `internal/genledger` appears in doc comments, mapping docs, plan records and scripts throughout this repo and none of those is an importer. **Proof:** a genledger importer added to `pkg/validation` turns the assertion RED (`["entities", "pkg/validation"]`) while **all three scans stay GREEN**, which is the demonstration that the tripwire is the only thing watching that boundary. The doc says the fix on that day is to widen the scans, never to relax the assertion.
  4. **MEDIUM** — `test(entities)` (`085ed2f`) — **the two scans were coupled: a call that escaped the parser scan was credited as path-guarded for free.** Path argument positions resolve from `clientMethodParameters`, which indexes the raw `*Client` only, so a call through any other spelling resolved to no positions, nothing was reported missing, and the call passed. Measured rather than reasoned about: a facade calling `GetSegmentByIDWithResponse` with three caller-controlled ids and **no guard at all** passed the pre-fix path-id scan and was caught only by the seam scan, one test away. Unknown-but-generated is now a FINDING in both scans. The residual dependency is real and is stated in BOTH doc comments — the path-id scan's coverage is conditional on the seam scan keeping foreign spellings out of the package — and the branch is documented as an invariant so it is not simplified back into a skip.
  5. **LOW, money path** — `fix(entities)` (`319cd55`) — **`readRawResponse` dereferenced `resp.Body` without a nil check.** A caller who discards the transport error (`resp, _ :=`) hands it `(nil, nil)` and a connection failure becomes a nil pointer dereference. The natural spelling does not compile, so reaching it takes a deliberate `_` — but the SDK bans panics in library code, and after the retrofit this one function is the funnel every single-object read and every write passes through, so the panic would land on the money path. Returns a plain sentinel the callers wrap like any other cause from here. **Mutation-checked:** with the guard removed the new test panics; with it, an error and no response.
  6. **LOW** — recorded, no code change — **the 10 MiB response-body cap is a fourth behaviour change the phase did not name.** Folded into the phase A entry above.
  7. **LOW** — `test(entities)` (`3f59eee`) — **the pin table's "dual family" row was not the dual SPELLING the commit message and the plan claimed was closed.** Both rows of the real pair added — `V1.Transactions.UpdateOperation` and `V1.Operations.UpdateTransactionOperation`, one generated operation under two facade names — which makes the claim true rather than reworded and adds the one shape the table lacked: a WRITE, reaching the shared decode through `writeJSON` rather than `readOne`. **Mutation-checked:** reverting one spelling to the generated parser turns that row alone red on both error properties, its sibling spelling stays green, and the seam scan independently names the site.
  - Gates green, in order: `go build ./...`, `make test`, `make check-codegen-drift` ("no codegen drift: committed clients reproduce from the specs"), `rm -rf ~/.cache/golangci-lint && make lint` (**0 issues — the first round in five where the cold linter did not bite**, which is what keeping every new function small bought), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`). `git status --short` clean.
  - **Method note, and it is the finding of this round — carried in from the contrarian.** Every mutation this epic has used to validate a test DELETES something: strip the guard, revert the facade, remove the row. That proves a test can SEE a site. **It never proves the test CHECKS the property.** All five escapes that worked here kept the guard call in place and deformed it into something that still LOOKS like a guard — moved below the call, wrapped in a condition, emptied of pairs, aimed at a shadowed name. A deletion-mutation cannot reach any of those, which is why four consecutive rounds of deletion-mutation left the vacuous-guard hole open and why `88b0503` could close it in letter and not in fact. **The rule for every future verification round: mutate toward the shape a tired contributor would actually write, not toward removal.** Guard below the call, guard inside an `if`, call hoisted to a local, name reused. Those are the diffs that get written at the end of a long day, and they are the ones a scan has to survive.

- [x] **Epic 4 phase A fix round 2** (2026-08-25, post-verification). Adversarial verification of round 1 returned FAIL: 1 HIGH, 1 MEDIUM, 2 LOWs — **every one of them again in the enforcement artefacts or in the round-1 fixes themselves, none in the 45-site retrofit.** Six commits, `137ffa1`..`5e34e51`, not pushed. The docs debt inventory is still untouched and stays open for phase B.

  The round ran under one rule, which is the round-1 HIGH's cause recurring: **when a fix is for a defect CLASS, apply it to every site with the shape, not the site the review named** — and enumerate the class before fixing, mechanically, saying how.

  1. **HIGH, money path** — `fix(entities)` (`137ffa1`) — **`readCount` had the identical nil-response dereference `319cd55` fixed in `readRawResponse`, and round 1 left it there.** It does not route through that helper and cannot: a HEAD count is headers-only, so there is no body to drain. It carried its own `defer resp.Body.Close()` behind an `err != nil` check alone, so `readCount(nil, nil)` panicked — on the shared decode for the transactions count and the six onboarding counts, in an SDK that bans panics in library code. Same guard, same sentinel, before the defer.
     - **Class enumeration, and it is the commit's substance.** A parser walks every non-test file in `entities/` and reports every function that RECEIVES the raw pair — an `*http.Response` followed by an `error` in its own parameter list, or a function-typed parameter whose RESULTS carry the same two (`writeJSON`'s `send`, the three transition helpers' `call`). **Sixteen members.** Each is probed with `(nil, nil)` in a table, and the table is asserted EQUAL to the enumeration, so a helper written tomorrow with this shape either gets a row or fails the test. Fifteen already refused a nil response; `readCount` was the one that did not, matching the verifier's measurement of exactly one.
     - **The enumeration earned its keep on the first run:** it found `HTTPClient.recordRequestFailure` — `(ctx, method, url, resp, elapsed, err)` — which the review's list did not name. One member in sixteen was invisible to a list and visible to a parser. ("Followed by", not "immediately followed by", is what catches it, and `writeJSON`'s `(*http.Response, []byte, error)` is why that is the right rule rather than a loose one.)
     - Deliberately NOT members, stated in the test: functions handed a response WITHOUT an error beside it (`statusOf`, `requestIDOf`, `decodeOne`, `guardListBody`, `closeResponseBody`, `safeHTTPLogAttrs`), which run downstream of the guard rather than at the entry point; functions that RETURN a response; and a func-typed parameter carrying the pair in its PARAMETERS (`SetCustomRetryPolicy`'s predicate), which is a callback the SDK invokes rather than a value handed in. **Adjacent and left alone, recorded here rather than silently:** `HTTPClient.closeResponseBody(nil)` would panic — it nil-checks its receiver and not its argument — but it is outside this class and ~~its single caller (`drainAndCloseResponseBody`)~~ **all THREE of its callers guard before calling** (corrected in fix round 3: `http_retry_response.go:298` inside `drainAndCloseResponseBody`, and `http.go:642` and `http.go:711`, both in deferred cleanup closures; every one tests `resp != nil && resp.Body != nil`, so `Body` is covered too, not just `resp`). Not fixed, because inventing scope is how a round loses its shape; recorded, because the next verifier should not have to rediscover it — and the count is corrected because a record that describes a tree with one caller sends the next auditor looking for a tree that does not exist.
     - **Mutation-checked:** with the guard removed the `readCount` row panics inside the deferred `Close`; with it, an error and no response. Every other row green either way.
  2. **MEDIUM** — `test(entities)` (`c3c055f`) — **the path-id scan checked that the guard RUNS, never that its verdict is USED.** This passes the round-1 scan and a cold lint run: `if err := requirePathIDs(op, ...all pairs...); err != nil { _ = err }`. Every pair named, depth 1, unconditional, before the call — and `id=".."` reaches the wire with a nil error, which is the scope escalation two Epic-2 rounds were spent closing (`".."` pops a segment, so deleting a ledger issues the DELETE on the organization).
     - **Class enumeration:** not by listing ways to defang a guard, but by taking the ONE spelling that is a guard and refusing everything else by construction. All 202 live call sites write `if err := requirePathIDs(...); err != nil { return ... }`, so that is now the only accepted shape — the call in an `if`'s INITIALISER, with a `return` among that `if` body's own statements. `_ = requirePathIDs(...)`, `err := requirePathIDs(...)` never checked, and `defer requirePathIDs(...)` all drop out without being named. No SSA needed. **Superseded twice: fix round 3, finding 1 — those are two of the shape's four parts, and the unpinned CONDITION and RETURNED VALUE let four more deformations through; all four parts are pinned now. And fix round 5, finding 2 — "refuse everything else by construction" was implemented as SKIPPING a refused call rather than reporting it, so a deformed guard hid behind a well-shaped sibling. Dropping out without being named is not the same as being refused, and a call the accepted shape does not cover now flags the function on its own.**
     - **Deformations re-run, each written as a compiling facade against the real generated client:** guard's error discarded — PASSED before, FLAGGED now; guard deferred — PASSED before, FLAGGED now; error assigned and never checked — PASSED before, FLAGGED now; `_ = requirePathIDs(...)` — PASSED before, FLAGGED now; the legitimate spelling — clean before and after. Against the PRE-fix scan that same probe file reported exactly ONE offender, the accepted false positive in finding 5.
  3. **LOW** — `test(entities)` (`9a5ea2d`) — **the presence-only branch credited a TOKEN guard.** A facade that names a path-parameter operation, forwards it as a function value to a package-local helper absent from `transitionHelpers`, and guards a value that never becomes a path segment passed, while the id that does become one was guarded by nothing. A fourth transition helper added tomorrow inherits that for free — which is the cost of keeping `transitionHelpers` a literal list, and the reason the CALL SITE has to carry its own check.
     - **Class enumeration:** the rule already existed one level down. `helperGuardsWhatItForwards` requires every plain identifier a KNOWN helper forwards to be guarded; the branch for an UNKNOWN helper required nothing of the kind. Same rule, applied at the call site, and both now run through one implementation (~~`forwardedIdentifiers`~~ — **superseded twice: fix round 3, finding 2 replaced it with `forwardedValues`, which compares every argument as source text instead of plain identifiers only; fix round 4, finding 2 then DELETED the call-site branch entirely**) instead of two — which also drops the positional "the context is argument 0" assumption the helper check carried.
     - One false positive found while probing and closed rather than accepted: a facade's own `const operation = "..."` is a forwarded plain identifier, fixed at compile time, so it cannot carry `".."` in from a caller. Local string constants are excluded; everything a caller can influence is not.
     - **Deformations re-run:** token guard + unknown helper — PASSED before, FLAGGED now; the same shape guarding the forwarded id — clean before and after. The three live transition helpers still pass.
  4. **LOW, two one-line scan holes** — `test(entities)` (`4877624`), each previously backstopped only by a lint rule that one `//nolint` disables. **(a)** the seam scan walked `*ast.SelectorExpr` only, so a DOT IMPORT of the generated package makes `ParseGetSegmentByIDResp` a bare identifier the scan cannot see: a compiling facade doing the raw call followed by the free-function parser reported **ZERO offenders**. Both parser SPELLINGS were already banned; only one of the two ways to WRITE either was being read. The walk now matches `*ast.Ident` alongside the selector (`isParserSpelling` already took a bare name) and stops descending on a match so a selector's own `Sel` is not counted twice. **(b)** `unconditionalCalls` collected `defer requirePathIDs(...)`: a `DeferStmt` keeps its call directly, with no block in between, so the block pruning never reached it. `*ast.DeferStmt` and `*ast.GoStmt` are now pruned beside `*ast.BlockStmt`. **Round-3 note: that walker (`unconditionalCalls`) is deleted. Pinning the initialiser to one assignment binding one identifier to a direct guard call makes a deferred, spawned or nested guard structurally impossible to credit, so the pruning it did by hand is now done by the shape — the closure survives, the code enforcing it does not.** **Class enumeration for each:** (a) the ban is on two NAMES, and qualified/bare are the only two ways to write either — one was unread; (b) `DeferStmt` and `GoStmt` are the complete set of statement kinds carrying a `CallExpr` with no intervening `BlockStmt`. **Both probes re-run and FLAGGED.** The deferred spelling is now refused twice over — finding 2's spelling requirement above it, and the helper underneath no longer collecting a postponed call.
  5. `docs(entities)` (`39aeaaf`) — **ceiling hygiene.** The scan's ceiling paragraph still described the previous round's state (two of five escapes open, three closed) while four more shapes had since been closed. It now lists exactly what is open — **a guarded variable REASSIGNED between guard and call, an inner scope SHADOWING a guarded name, and reflection** — and nothing else. Accepted strictness gains its second entry beside the `strings.ToLower` one: **a guard placed after a generated call that sits inside a DEFERRED CLOSURE is reported even though the guard really does run first.** Accepting it means reasoning about execution order instead of source position, which is the reasoning that let a guard below a call pass in the first place; a generated call belongs in a facade's body, not in a defer.
  6. `style(entities)` (`5e34e51`) — cold-linter fixes, **fourteen findings and eleven of them the same `thelper` shape, for the fifth round running.** Round 1 was the only round in five the cold linter left alone and this round put the shape straight back; the warm linter passed it again, as it has every time. Fixed at the type rather than by sprinkling `t.Helper()`: the probe closure takes `require.TestingT`, which is what it is — an assertion closure, not a subtest body. The other three were `nilnil` and `bodyclose` on the two stubs whose entire purpose is to hand back nothing (a `//nolint` each, with the reason) and `prealloc` on a two-part slice build, now `slices.Concat` over two literals.
  - Gates green, in order: `go build ./...`, `make test`, `make check-codegen-drift` ("no codegen drift: committed clients reproduce from the specs"), `rm -rf ~/.cache/golangci-lint && make lint` (0 issues), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`). `git status --short` clean. Scan totals unchanged throughout: **0 parser call sites, 225 operations reached raw, 209 guarded facade functions across 205 path-parameter operations** — every stricter criterion covers the same surface rather than a smaller one.
  - **Method note.** Round 1's rule was *mutate toward the shape a tired contributor would write, not toward removal*, and it found five escapes. This round shows the rule has a second half: **a deformation that passes tells you the scan is weak; only re-running it after the fix tells you the fix is the right size.** Three of the four findings here were fixed by identifying the ONE legitimate shape and refusing everything else by construction — the guard's spelling, the parser's two names, the statement kinds that carry a postponed call — rather than by listing the deformations and banning them one at a time. A ban-list is the same mistake as a sample: it covers the examples someone thought of. The complement of a legitimate shape covers the ones nobody has written yet.

- [x] **Epic 4 phase A fix round 3** (2026-08-25, post-verification). Adversarial verification of round 2 returned FAIL: 1 HIGH, 1 MEDIUM, 2 LOWs — **for the third round running, every finding is in the enforcement artefacts or in the previous round's fixes, none in the 45-site retrofit.** Two fix commits, `5f4b254` and `d19b624`, plus this record's own commit, which carries finding 3 because finding 3 IS a correction to this plan. Not pushed. The docs debt inventory is still untouched and stays open for phase B.

  1. **HIGH, money path, and it absorbs one of the LOWs** — `test(entities)` (`5f4b254`) — **round 2 defined "the one accepted guard spelling" by two of its four parts and left the other two unspecified.** The scan required the guard in an `if`'s INITIALISER and a RETURN among the body's statements, and said nothing about the CONDITION between them or the VALUE the return carries. Four deformations kept both accepted parts and put `id=".."` on the wire with a nil error — the scope escalation two Epic-2 rounds were spent closing, since `".."` pops a path segment and a DELETE on a ledger becomes a DELETE on the organization:

     ```go
     if err := requirePathIDs(op, ...); err == nil { return nil, err }               // returns only when the ids are FINE
     if err := requirePathIDs(op, ...); err != nil && f.strict { return nil, err }   // returns only when a flag agrees
     if err := requirePathIDs(op, ...); ctx.Err() != nil { return nil, err }         // tests something unrelated
     if err := requirePathIDs(op, ...); err != nil { _ = err; return nil, nil }      // stops, and returns (nil, nil)
     ```

     The fourth is the absorbed LOW: the request does stop, and the caller gets a nil error for a rejected id — the silent-zero defect wearing a guard's clothes. All four read, at a glance, exactly like a guard.
     - **Class enumeration:** not a ban-list of the four, but the complement of ONE fully specified shape. All four parts of `if <err> := requirePathIDs(...); <err> != nil { return ..., <err> }` are now pinned — the Init binds exactly one identifier to a **direct** call to the guard, the Cond is exactly `<that identifier> != nil` (a `BinaryExpr`, `token.NEQ`, the guard-err ident against `nil`, nothing AND-ed or OR-ed on), ~~the Body returns directly~~, and that return carries the guard's own error among its results. **Superseded by fix round 4, finding 1: "the Body returns directly" was NAMED here and never constrained — `returnsIdent` looked for A return among the body's statements and let anything precede it. Three more deformations walked through. The body must now be EXACTLY ONE statement and that statement must be the return (`returnsOnlyIdent`).** A wrapper around the guard call (`err := wrap(requirePathIDs(...))`) drops out of rule 1 for free, and so does everything the old hand-written walker pruned by name — `_ = require...`, `defer require...`, `go require...`, a guard in the condition rather than the initialiser. **`unconditionalCalls` and `returnsDirectly` are deleted**: what they enforced is now structurally impossible, so the fix is a net simplification rather than another layer.
     - **Live-tree check rule 4 forced, done BEFORE committing:** all 202 live sites return the **bare** identifier — 161 `return nil, err`, 29 `return err`, 12 `return 0, err`, counted mechanically against the tree — so the strict form needs no widening for a wrapped error. **The accepted-strictness list gains its third entry:** a guard whose body returns `fmt.Errorf("%s: %w", operation, err)` is refused, with the honest widening ("the identifier appears WITHIN one of the results") recorded there for whoever needs it, so it lands in the scan's rule rather than in a `//nolint` at the site.
     - **Deformation table** (compiling facades against the real generated client): inverted condition — PASSED before, FLAGGED now; condition AND-ed with a flag — PASSED before, FLAGGED now; condition testing `ctx.Err()` — PASSED before, FLAGGED now; verdict swallowed in the return — PASSED before, FLAGGED now; guard call wrapped (`wrap(requirePathIDs(...))`, added while probing) — PASSED before, FLAGGED now; the ordinary spelling — clean before and after; the LINE-WRAPPED spelling at `operations_v2_facade.go:93,110` — clean before and after.
  2. **MEDIUM** — `test(entities)` (`d19b624`) — **the delegation-site branch tracked plain identifiers only, and its doc comment justified that with a claim that was factually wrong.** `forwardedIdentifiers` collected `*ast.Ident` arguments and silently dropped every other AST shape, so a selector (`ids.ID`) or an index (`ids[0]`) forwarded to a helper outside `transitionHelpers` reached the wire unguarded with nothing reporting it, while a token guard on an unrelated value satisfied the scan. The comment said the DIRECT-call comparison also accepted identifiers only; that comparison has always compared `types.ExprString` of whatever expression sat in a path position and reports any unmatched one. **The two branches disagreed and the delegation branch was the loose one** — a selector and an index are not exotic, they are what a caller writes the day a facade carries its ids in a struct or a slice. **Superseded by fix round 4, finding 2: the call-site delegation branch is DELETED. It had zero live users and four further escapes; an unknown delegation is now reported rather than judged. `forwardedValues` survives and serves `helperGuardsWhatItForwards` alone.**
     - **Class enumeration:** stop enumerating what to INCLUDE (which is what "plain identifiers" was) and enumerate what to EXCLUDE, with a reason each. Every argument is now compared as source text whatever its shape, minus four things that provably cannot become a path segment: the **context** (by name, not position), **literals and local string constants** (fixed at compile time — the `const operation = "Segments.Get"` every facade declares), the **generated operation being delegated** (a function value, matched by resolving the selector against `pathOps`, which is precisely why `ids.ID` is *not* excluded with it), and the **variadic spread at the tail** (`idempotencyEditorsTracer(ctx, false)...`, forwarded by all three live helpers; oapi-codegen never puts a path parameter in a variadic tail). `helperGuardsWhatItForwards` shares the implementation, so the inside of a transition helper and its call sites are judged by one rule, not two.
     - **Deformation table:** selector forwarded (`ids.ID`) — PASSED before, FLAGGED now; index forwarded (`ids[0]`) — PASSED before, FLAGGED now; local rename between guard and delegation (`x := id`) — FLAGGED before and after; the same delegation guarding what it forwards — clean before and after. The three live transition helpers pass throughout.
  3. **LOW, docs** — this commit — **the round-2 record said `HTTPClient.closeResponseBody(nil)` was safe because "its single caller guards". There are THREE callers**, all guarding: `http_retry_response.go:298` (inside `drainAndCloseResponseBody`), `http.go:642` and `http.go:711` (both in deferred cleanup closures). Every one tests `resp != nil && resp.Body != nil`, so `Body` is covered and not just `resp`. The verdict is unchanged — still out of the nil-pair class, still not fixed — but a record that describes a tree with one caller sends the next auditor looking for a tree that does not exist.
  - Gates green, in order: `go build ./...`, `make test`, `make check-codegen-drift` ("no codegen drift: committed clients reproduce from the specs"), `rm -rf ~/.cache/golangci-lint && make lint` (0 issues — **second round running the cold linter did not bite**, which is what keeping every new function to one job bought: `guardAssignment`, `comparesNotNil`, `returnsIdent` — **renamed `returnsOnlyIdent` in fix round 4, finding 1** —, `isIdent`, `isVariadicSpread`, `isFixedArgument`), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`). `git status --short` clean — the probe file carrying the deformations is deleted, as in every prior round. Scan totals unchanged throughout: **0 parser call sites, 225 operations reached raw, 209 guarded facade functions across 205 path-parameter operations.**
  - **Method note, and it is the finding of this round.** Round 2's rule was *fix by taking the ONE legitimate shape and refusing everything else by construction, rather than by listing deformations*. That rule is right and it is what let round 2's fix be small — but round 2 applied it to a shape it had only half-specified, and the complement of a half-specified shape is a half-closed hole. **The complement of a shape is only closed when EVERY part of the shape is pinned.** For an `if` guard that is four parts: initialiser, condition, body, and what flows out through the return; leaving the condition unspecified left three escapes that each kept the two pinned parts intact. The generalisation for the next round: **after choosing the one accepted form, write out its parts and check that the matcher constrains each one.** A part nobody named is a part nobody closed, and it will read as closed because the parts around it are.
- [x] **Epic 4 phase A fix round 4** (2026-08-25, post-verification). Adversarial verification of round 3 returned FAIL: 1 HIGH, 1 MEDIUM, 2 LOWs — **for the fourth round running, every finding is in the enforcement artefacts or in the previous round's fixes, none in the 45-site retrofit.** Three fix commits, `d182ada`, `5389037`, `7bf9806`, plus this record's own commit, which carries finding 4 because finding 4 IS a correction to this plan. Not pushed. The docs debt inventory is still untouched and stays open for phase B.

  1. **HIGH, money path** — `test(entities)` (`d182ada`) — **round 3 NAMED part 3 of the accepted guard shape and never constrained it.** Its own rule list says "the Body returns directly"; `returnsIdent` then looked for A return among the body's statements and let anything precede it. Three deformations kept the three genuinely-pinned parts — the direct-call initialiser, the `err != nil` condition, a return carrying the bare identifier — and walked straight through:

     ```go
     if err := requirePathIDs(op, ...); err != nil { err = nil; return nil, err }        // caller gets (nil, nil)
     if err := requirePathIDs(op, ...); err != nil { var err error; return nil, err }    // caller gets (nil, nil)
     if err := requirePathIDs(op, ...); err != nil {
         bad, badErr := f.tracer.DeleteRule(ctx, id)                                     // the REJECTED id, on the wire
         _, _ = bad, badErr
         return nil, err
     }
     ```

     The first two are the silent-zero defect re-entered one statement earlier than the rule that closed it for the return's own results: a rejected id reads as success. The third is worse — the caller gets the honest validation error while `DELETE /v1/rules/{id}` has already left with `id=".."`, which pops a path segment. That is the scope escalation two Epic-2 rounds were spent closing, reached from INSIDE the guard that exists to prevent it.
     - **Class enumeration:** the body is now EXACTLY ONE statement and that statement is the return (`returnsOnlyIdent`). **Live-tree check done BEFORE committing: 202 of 202 guards already have a single-statement body**, counted mechanically, so the strictness costs nothing. Anything a facade genuinely needs to do on a rejected id belongs after the guard rejects, not between the rejection and the return.
  2. **MEDIUM** — `test(entities)` (`5389037`) — **the delegation-credit branch had four escapes and ZERO live users. Deleted rather than repaired.** Branch usage measured against the live tree: direct call **199**, known transition helper **10**, delegation-credit **0** — the 209 total is entirely the first two. The four escapes it carried: (1) the operation **hoisted** into a local first, so the delegation call carried a plain identifier and the selector-only match saw no delegation at all — the same hoist that defeated the direct-call branch and the sibling delete-seam scan before it; (2) **no ordering requirement**, the guard collected to the function's closing brace, so a guard written AFTER the delegation credited it; (3) the **variadic-tail exclusion**, sound for a GENERATED method (oapi-codegen puts request editors there) and unsound applied to a LOCAL helper whose tail the author writes — `ids...` carried the id through; (4) the **context exclusion is by NAME**, so a value spelled like the context parameter was dropped without being compared.
     - **Class enumeration, and the deletion IS the enumeration:** closing four escapes in a branch nothing uses buys nothing and has to be maintained. Unknown is now FLAGGED — the rule the sibling seam scan already follows for a spelling it cannot resolve, and that `unguardedPathArguments` follows for an unrecognised generated spelling. All four close by construction, and the scan lost 119 lines against 80 added. **Amended in fix round 5, finding 1: the report this installed was UNREACHABLE from the direct-call branch — any direct call at all credited before `unknownDelegation` could run, so an operation named beside an honest direct read was dropped. The deletion and the rule were both right; the placement was not, and the checks are no longer alternatives.**
     - **The cost, and it is the friction `transitionHelpers` exists to create:** a facade delegating to a NEW local helper is refused until someone adds that helper to the literal list, where `helperGuardsWhatItForwards` then checks its interior. The list is literal on purpose — "adding a fourth helper here should be a decision someone makes on purpose" — and this makes that decision unavoidable rather than optional. Probed: an HONEST delegation to an unknown helper, guarding exactly what it forwards, is now reported. That is the deliberate false positive, and its fix is one line in `transitionHelpers`.
     - **Residual, recorded not fixed:** `forwardedValues` survives and now serves `helperGuardsWhatItForwards` alone. Its context exclusion is still by NAME rather than by TYPE, and its variadic exclusion still assumes the tail is oapi-codegen's request editors. Both are sound there, because the only call it reads is a call to a known helper's function-typed parameter — the generated method — and the three helpers are a list a human curates.
  3. **LOW** — `test(entities)` (`7bf9806`) — **a parenthesised condition was an undocumented false positive.** `if err := requirePathIDs(...); (err != nil) { return nil, err }` is the live guard with one pair of parentheses added; `comparesNotNil` saw an `*ast.ParenExpr` and reported the facade as unguarded. Closed rather than documented, and closed as a class: `ast.Unparen` at every position the matcher inspects — the condition itself, and both operands and the return's results through `isIdent`. `ast.Unparen` strips grouping and nothing else, so it admits no value the matcher would otherwise refuse; checked with a parenthesised DEFORMED condition (`(err != nil) && f.strict`), which is flagged before and after.
  4. **LOW, docs** — this commit — **round 2's finding 3 still presented `forwardedIdentifiers` as its fix.** Round 3 replaced it with `forwardedValues` and applied the supersession one line up but not there; round 4 then deleted the branch entirely. Marked in place, along with three more references the round's own deletions and renames invalidated: round 3's "the Body returns directly" rule (superseded by finding 1), round 3's finding 2 class enumeration (the branch it describes is gone), and `returnsIdent` in round 3's gate line (renamed `returnsOnlyIdent`).
  - **Deformation table, before → after** (compiling facades against the real generated client, in one probe file deleted before the gates):

    | Probe | Before | After |
    |---|---|---|
    | guard body nils the error (`err = nil; return nil, err`) | PASSED | FLAGGED |
    | guard body shadows the error (`var err error; return nil, err`) | PASSED | FLAGGED |
    | guard body fires `DeleteRule` with the rejected id | PASSED | FLAGGED |
    | delegation with the operation hoisted into a local | PASSED | FLAGGED |
    | delegation guarded AFTER the call | PASSED | FLAGGED |
    | delegation carrying the id in an author-written `ids...` tail | PASSED | FLAGGED |
    | delegation carrying the id under the context parameter's name | PASSED | FLAGGED |
    | honest delegation to an unknown helper (deliberate false positive) | PASSED | FLAGGED |
    | parenthesised condition `(err != nil)` | FLAGGED | clean |
    | parenthesised operands and result `(err) != (nil)` / `return nil, (err)` | FLAGGED | clean |
    | parenthesised DEFORMED condition `(err != nil) && f.strict` | FLAGGED | FLAGGED |
    | the ordinary live spelling | clean | clean |
    | the LINE-WRAPPED live spelling | clean | clean |

  - **Per-part deformation table for the final matcher.** The accepted shape is an `if` with four parts; each row violates EXACTLY ONE of them and keeps the other three intact. All four flag:

    | Part | Deformation violating only that part | Verdict |
    |---|---|---|
    | 1. Init binds one identifier to a DIRECT guard call | `if err := probeWrap(requirePathIDs(op, "id", id)); err != nil { return nil, err }` | FLAGGED |
    | 2. Cond is exactly `<that identifier> != nil` | `if err := requirePathIDs(op, "id", id); err == nil { return nil, err }` | FLAGGED |
    | 3. Body is exactly one statement, and it is a return | `if err := requirePathIDs(op, "id", id); err != nil { probeLog(); return nil, err }` | FLAGGED |
    | 4. that return carries the bare guard identifier | `if err := requirePathIDs(op, "id", id); err != nil { return nil, fmt.Errorf("%s: %w", op, err) }` | FLAGGED |

    Row 3 is the row round 3 never wrote, and it is the whole finding: the round probed the condition three ways and the results once, so parts 2 and 4 were proven and part 3 was assumed.
  - Gates green, in order: `go build ./...`, `make test` ("All tests completed successfully"), `make check-codegen-drift` ("no codegen drift: committed clients reproduce from the specs"), `rm -rf ~/.cache/golangci-lint && make lint` (0 issues — **the cold linter bit once**, `unused-receiver` on the new `unknownDelegation`, which needs no receiver and is now a plain function; folded into finding 2's commit rather than left as a trailing style commit), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`). `git status --short` clean — the probe file carrying the deformations is deleted, as in every prior round. Scan totals unchanged throughout: **0 parser call sites, 225 operations reached raw, 209 guarded facade functions across 205 path-parameter operations.** Zero behaviour change in product code: this round touched the scan test and this plan, nothing else.
  - **Method note, and it is the finding of this round.** Round 3's rule was *the complement of a shape is only closed when EVERY part of the shape is pinned*, with the generalisation *write out the parts and check the matcher constrains each one*. Round 3 then wrote out four parts, constrained three, and shipped — because "write out the parts" is something you do by reading your own matcher, and a matcher you wrote reads as doing what you meant it to do. **Naming a part is not constraining it, and the only proof that a part is constrained is a deformation that violates that part alone.** So the rule gets a mechanical second half: **after writing an N-part matcher, write N single-part deformations, one per part, each violating exactly one part, and confirm each one flags.** N probes, no judgement calls, no reading your own code charitably. Round 3's probes were four deformations of the condition and one of the return's results — thorough on two parts, silent on a third, and the silence read as coverage because the parts around it were covered.

- [x] **Epic 4 phase A fix round 5** (2026-08-25, post-verification). Adversarial verification of round 4 returned FAIL: 1 HIGH — **inside a change round 4 itself made** — 1 MEDIUM, 3 LOWs. **For the fifth round running, every finding is in the enforcement artefacts or in the previous round's fixes, none in the 45-site retrofit.** Four fix commits, `f40dc5d`..`0944eec`, plus this record's own commit. Not pushed. The docs debt inventory is still untouched and stays open for phase B.

  1. **HIGH, money path** — `test(entities)` (`f40dc5d`) — **the direct-call branch short-circuited the report round 4 had just written.** `classify` collected the operations a function NAMES, and then, the moment any direct call existed, entered the direct branch, examined only the direct calls, and credited. An operation named but NOT directly called was dropped on that path, and `unknownDelegation` — round 4's whole finding 2 — was **unreachable from it**. The deformation that proved it keeps everything honest except the one thing nobody looks at: an honest guard, an honest direct read of `GetRule`, and beside them a package-local helper handed `f.tracer.DeleteRule` plus an **unguarded** `cascadeID`. The function was credited, **all six structural scans stayed green**, and the wire carried `DELETE /v1/rules/{id}` with an unguarded id — the scope escalation two Epic-2 rounds were spent closing, since `".."` pops a path segment. Its read-only twin (delegating `GetLimit` instead) behaves identically, so nothing about the finding depends on the delegated call being destructive. **Live exposure: zero.**
     - **Class enumeration:** stop letting a branch DECIDE and make every function run every check. `offences` runs all three and returns all of them; no check returns early past another. An operation is accounted for by exactly two things — a **direct call**, whose arguments `unguardedPathArguments` then compares against the guard (including one reached through a hoisted local), or being **named in a call to a known transition helper**, whose interior `helperGuardsWhatItForwards` checks. Everything else is reported. **Superseded in fix round 6, finding 1: "an OPERATION is accounted for" was the defect, not the wording. Accounting was keyed by operation NAME, so one honest mention laundered every other mention of the same name in the same function, and "everything else is reported" was false for every mention after the first. Accounting is now a property of the mention NODE — the target of a direct call, or a direct element of a known transition helper's argument list — and every other mention is reported, same-named or not.** The old boolean `delegatesToTransitionHelper` is deleted with the branch: it asked whether the function called a transition helper ANYWHERE, which credited every operation the function named the moment one of them was delegated; the operation must now be named IN the delegating call.
     - `unguardedPathArguments` gains one early return (no direct calls, no argument expressions to compare) and it is the one exit in the file that skips a check — documented at the exit, and safe because `operationsNotReached` runs on the same function and reports exactly what it would have missed. **Re-derived in fix round 6 under per-mention accounting and it holds, for a reason the old comment did not state: the mention check walks the function body ITSELF, independently of the call list, so a function with zero direct calls still has its mentions accounted. What the exit declines is the argument loop, whose input is `call.Args` — empty by construction here. `operationsNotReached` is now `unaccountedMentions`; the comment at the exit names it and states the emptiness argument.**
  2. **MEDIUM** — `test(entities)` (`b4355e5`) — **a deformed guard placed BEFORE a well-shaped one re-entered the silent zero.** `guardedValues` SKIPPED a `requirePathIDs` call it refused, as if it were not there, and unioned the values from the guards it accepted. So the verdict depended on ORDER: write the deformed guard first (`err = nil; return nil, err` — refused since round 4 because its body is two statements) and the well-shaped one second, and the scan credits via the second while the RUNTIME takes the first. The caller gets `(nil, nil)` for a rejected id and the wire carries it. Both orders passed, because skipping is order-blind in both directions.
     - **Class enumeration:** refusal now means refusal. A `requirePathIDs` call anywhere in a function that is not the initialiser of an accepted depth-1 guard makes that function an offender **on its own**, whatever else in it guards. That is the same unknown=flag rule round 4 applied to delegations, applied to guard SPELLINGS — round 2's "refuse everything else by construction" is not satisfied by looking away. It runs BEFORE the operation gate, so it also reaches the three **transition helpers**, which name no operation at all and are the more expensive place to leave the hole (thirty-odd facade methods are credited on their promise). Mutation-checked there: a deformed guard inserted above `ruleTransition`'s real one is named by line number; reverted.
     - **The else-if / nesting ceiling note is reconciled deliberately, and the strict way.** A well-shaped guard nested inside an `if`, an `else if`, a loop, a switch case or a closure is now **REPORTED** rather than silently uncredited — even when its own four parts are perfect and a depth-1 guard already covers the same value. The alternative rule (judge the four parts wherever the guard sits, let depth decide only whether it CREDITS) keeps a redundant nested guard clean at the cost of a second, weaker acceptance test living beside the first, and two tests for one shape is how the last four rounds went wrong. **Live cost: zero — all 202 guards are statements of their own function's body, counted mechanically (`awk` over the leading tabs) before the rule landed.** Recorded in the accepted-strictness list as a deliberate false positive with its one-line fix.
  3. **LOW ×2, docs** — `docs(entities)` (`846ed20`) — **two refused-but-legitimate spellings were absent from the accepted-strictness list**, which said three where the matcher refuses six. Both were confirmed refused against the real generated client before being written down, and neither is accepted into the matcher: **(a)** a **naked return under named results** (`if err = requirePathIDs(...); err != nil { return }`) — rule 4 has nothing to match, and that is the point rather than an oversight: rule 4 exists to pin what flows OUT of the guard, and a naked return hands back whatever the named results hold at that instant, which is precisely what `err = nil; return nil, err` did. Reading it needs dataflow. 202 of 202 live guards name their results. **(b)** a **block-wrapped return** (`{ { return nil, err } }`) — the body's single statement is a block, and widening to descend through blocks looking for A return is the exact rule round 4 deleted after it let a rejected id reach the wire from INSIDE the guard. The third addition is this round's own nesting strictness (finding 2). Each entry carries the one-line change a contributor makes at the site, so the fix lands there and not in a loosened match.
  4. **LOW, ceiling** — `docs(entities)` (`0944eec`) — **a `RequestEditorFn` can rewrite the outbound path, and the ceiling did not say so.** Every generated method takes a `...RequestEditorFn` tail; an editor is `func(context.Context, *http.Request) error`, holds the built request, and can assign to `req.URL.Path` on any branch — after the guard has run and after the request builder styled its parameters. A closure capturing a caller value therefore puts that value in the path without it ever sitting at a path ARGUMENT POSITION, which is the only place `pathArgumentsOf` looks. **Not hypothetical scaffolding:** `setQueryParam` (`entities/organizations_facade.go:243-252`) is a hand-written editor in this package — harmless (only `req.URL.RawQuery`, literals at its one call site) and one field away from the shape. Recorded as the FOURTH ceiling entry, with the honest closure: resolve each editor value to its body and read that body for writes to `req.URL`, which is type-checked analysis, not a stricter match. **The variadic-exclusion justification in `forwardedValues` is corrected in the same commit:** it argued from the SIGNATURE (oapi-codegen never puts a path parameter in the tail — true) and was silent about the EFFECT, which let it read as the stronger claim that the tail cannot influence the path. It now says both and points at the ceiling entry for what the exclusion costs.
  - **Deformation table, before → after** (compiling facades against the real generated client, in one probe file deleted before the gates):

    | Probe | Before | After |
    |---|---|---|
    | honest guard + honest read + `DeleteRule` and an unguarded id to an unknown helper (B8D) | PASSED | FLAGGED |
    | the same with a READ (`GetLimit`) delegated — B8D's read-only twin | PASSED | FLAGGED |
    | deformed guard BEFORE a well-shaped one for the same value | PASSED | FLAGGED |
    | well-shaped guard BEFORE a deformed one (the opposite order) | PASSED | FLAGGED |
    | well-shaped guard NESTED beside a depth-1 guard (deliberate false positive) | clean | FLAGGED |
    | naked return under named results (deliberate false positive) | FLAGGED | FLAGGED |
    | block-wrapped return (deliberate false positive) | FLAGGED | FLAGGED |
    | honest delegation to an unknown helper (round 4's deliberate false positive) | FLAGGED | FLAGGED |
    | well-shaped guard placed BELOW the call (round 1's closure) | FLAGGED | FLAGGED, as an unguarded argument and not as a deformed guard |
    | a pure direct-call facade, guarded honestly | clean | clean |
    | delegation to a known transition helper | clean | clean |
    | deformed guard inside `ruleTransition` itself (product-code mutation, reverted) | PASSED | FLAGGED |

  - Gates green, in order: `go build ./...`, `make test` ("All tests completed successfully"), `make check-codegen-drift` ("no codegen drift: committed clients reproduce from the specs"), `rm -rf ~/.cache/golangci-lint && make lint` (**0 issues — the cold linter did not bite**, which is what keeping every new function to one job bought: `offences`, `operationsNotReached`, `transitionHelperCalls`, `calleeName`, `deformedGuardCalls`, `deformedGuardOffence`), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`). `git status --short` clean — the probe file carrying the deformations is deleted, as in every prior round. Scan totals unchanged throughout: **0 parser call sites, 225 operations reached raw, 209 guarded facade functions across 205 path-parameter operations** — every stricter criterion covers the same surface rather than a smaller one. Zero behaviour change in product code: this round touched the scan test and this plan, nothing else.
  - **Method note, and it is the finding of this round.** Round 4's rule was mechanical and right: *after writing an N-part matcher, write N single-part deformations, one per part, and confirm each flags.* Round 4 did exactly that, and its own HIGH is the next lesson: **the per-part table proves each PART in isolation and says nothing about how the parts COMPOSE, because parts compose through BRANCHES.** The escape here violated no part of the guard shape — the guard was perfect — it took an early exit that credited before a different check could run. A per-part table cannot see that; nothing that probes the matcher can, because the hole is in the control flow around it. **So the rule gets a second artefact beside the table: walk the classifier's control flow and ask, at every early exit, what this exit SKIPS for the inputs that take it.** An exit that skips a check for a subset it was never meant to exempt is this round's HIGH, exactly. The mechanical form: an early return is allowed only when what it skips is provably empty for the inputs reaching it, and that reason belongs in a comment at the exit — the file now has exactly one such exit, and it carries one.

- [x] **Epic 4 phase A fix round 6** (2026-08-25, post-verification). Adversarial verification of round 5 returned FAIL: 1 HIGH — **inside a change round 5 itself made, for the second round running** — plus 2 ceiling NOTES. **For the sixth round running, every finding is in the enforcement artefacts or in the previous round's fixes, none in the 45-site retrofit.** Two fix commits, `d542e01`..`70081b7`, plus this record's own commit. Not pushed. The docs debt inventory is still untouched and stays open for phase B.

  1. **HIGH, money path** — `test(entities)` (`d542e01`) — **the accounting union was keyed by operation NAME, so one honest mention laundered every other mention of that name in the same function.** Round 5's `operationsNotReached` built `reached` as a set of NAMES: the names some direct call reached, unioned with every name appearing anywhere inside a call to a known transition helper, compared against the names the function mentions. A name in the set was satisfied for the whole function, however many times it appeared and wherever those appearances sat. Three escapes, each compiling, each credited, each putting an unguarded id on the wire:
     - **(a) same operation, direct call plus a second handoff.** `f.tracer.DeleteRule(ctx, id)` called under an honest four-part guard, and beside it `probeCascade(f.tracer.DeleteRule, cascadeID)` handing the SAME operation to a package-local helper with an **unguarded** cascade id. The direct call had already put `DeleteRule` in the set, so the handoff was invisible. The wire carried `DELETE /v1/rules/{id}` with an unguarded id — the scope escalation two Epic-2 rounds were spent closing, since `".."` pops a path segment. Its **read-only twin** (a fallback handoff of `GetRule` beside an honest `GetRule` read) behaves identically, so nothing about the finding depends on the delegated call being destructive.
     - **(b) a DIFFERENT operation nested inside a transition call.** `pathOperationsNamedBy(call, pathOps)` inspected the ENTIRE transition-helper `CallExpr`, so an operation buried in a nested call at an argument position was credited to a helper that never receives it: `ruleTransition(ctx, op, f.tracer.ActivateRule, probePick(f.tracer.GetRule, otherID))` credited `GetRule`, and `GET /v1/rules/{id}` carried `otherID` unguarded.
     - **(c) same operation, transition call plus handoff.** The (a) shape with the honest mention credited by a transition helper instead of by a direct call — `probeSneak(f.tracer.ActivateRule, otherID)` beside `ruleTransition(ctx, op, f.tracer.ActivateRule, id)`. `POST /v1/rules/{id}/activate` carried `otherID` unguarded. This function has **zero direct calls**, so it is also the shape that takes the one early exit in `unguardedPathArguments`.
     - **Class enumeration:** there is no name match strict enough, because the offending mention and the honest one carry the same name by construction. **"Accounted" is now a property of the NODE, never of the symbol.** The scan walks the `*ast.SelectorExpr` nodes naming a path operation and accounts for a node only when it is (1) the `Fun` of a direct call, whose arguments `unguardedPathArguments` then compares against the guard, or (2) ITSELF a direct element of a known transition helper's `Args` — not merely contained somewhere inside one. Every other mention node is reported, same-named or not, nested or top-level, with the LINE it sits on, because a function can mention one operation several times and the reader needs the occurrence. `operationsNotReached` and `unknownDelegation` are replaced by `unaccountedMentions` + `accountedMentionNodes` + `unaccountedMentionOffence`; the `ops []string` parameter drops out of `offences` with them. Parentheses are read through on the delegating side only (`ast.Unparen` on each argument), on the same reasoning `isIdent` already gives: they carry no meaning, and refusing `ruleTransition(ctx, op, (f.tracer.DraftRule), id)` would be a pure false positive. Nothing else is read through.
     - **The early exit at `unguardedPathArguments` was re-derived, not assumed, and it stays.** Round 5 justified it by "`operationsNotReached` runs on the same function", which under the name-keyed model was *false for exactly the inputs that take the exit* — escape (c) takes it, and the union credited it anyway. Under per-mention accounting the justification is sound and stated at the exit: `unaccountedMentions` walks `fn.Body` itself, independently of the call list, and `offences` calls it on every function; what the exit declines is the argument loop, whose input is `call.Args`, empty by construction when there are no calls. Proven, not argued: probe P2 takes the exit and is FLAGGED.
     - **One new deliberate false positive, and it is the seventh.** An operation HOISTED into a local and then honestly called through it (`get := f.tracer.GetRule; get(ctx, id)`) is now reported, because the mention on the binding's right-hand side is neither a call target nor a transition argument. Accepting it means crediting a mention because some LOCAL derived from it is later called — the same laundering one level of indirection along, and it reopens escape (a) through a local this scan cannot attribute at all. `directPathCalls` still follows the binding, so the hoisted call's arguments still reach the identity check; that can only ADD findings and can no longer change a verdict. Recorded in the accepted-strictness list with its one-line fix (call the operation directly), and `hoistedOperations` says so at its own definition.
  2. **NOTES ×2, ceiling** — `docs(entities)` (`70081b7`) — **two ways to defeat the guard by NAMING, both credited today, both empirically confirmed before being written down.** (a) A **hoisted guard VALUE** — `g := requirePathIDs`, then `g(op, "id", id)` — is invisible to `deformedGuardCalls`, which matches `call.Fun` against the literal identifier: the call is neither accepted nor refused, simply unseen. So a **deformed** hoisted guard written above a well-shaped direct one re-enters the silent zero the round-5 refusal closed, in the order it closed it — the runtime takes the first, the scan credits the second. (b) A local **SHADOW of the guard FUNCTION name** (`requirePathIDs := func(_ string, _ ...string) error { return nil }`) makes every guard below it vacuous while all four parts of the accepted shape stay perfect. Recorded as the FIFTH ceiling entry and as a second clause on the existing SHADOWING entry (which covered shadowed VALUE names only — the same defect one level apart); "Four things remain out of reach" is now Five. **Neither closes with a stricter text match: the hostile spelling and the honest one are the same characters.** Both need identifier resolution — a type-checked pass resolving every name to its definition — which is the same tool the first two entries already name, and the ceiling paragraph now says so for all four non-reflection entries. **Live exposure: zero** — nothing in the tree hoists or shadows the guard, checked mechanically, and neither carries any innocent meaning in a facade.
  - **Deformation table, before → after** (compiling facades against the real generated client, in two probe files deleted before the gates; "PASSED" means silently credited):

    | Probe | Before | After |
    |---|---|---|
    | P11 cascade — same op, honest direct DELETE + handoff to an unknown helper with an unguarded id | PASSED | FLAGGED |
    | P1 fallback — P11's read-only twin (`GetRule`), so the class does not depend on the delegated call being destructive | PASSED | FLAGGED |
    | P2 transition + unknown — same op, honest transition delegation + handoff; **zero direct calls, takes the early exit** | PASSED | FLAGGED |
    | P8 nested-in-transition-call — a DIFFERENT op one level deep in the transition call's own arguments | PASSED | FLAGGED |
    | round-5 B8D — a different op handed to an unknown helper beside an honest guard and an honest read | FLAGGED | FLAGGED |
    | **new, single property** — a method value bound to a local and NEVER called (`del := f.tracer.DeleteRule`), nothing else in the function | FLAGGED | FLAGGED |
    | **new, single property** — an op hoisted and honestly CALLED through the local, under an honest guard (the new deliberate false positive) | clean | FLAGGED |
    | **new, single property** — an honest transition delegation whose op sits inside a CONVERSION rather than as a direct argument | clean | FLAGGED |
    | pure honest facade — guard + direct call | clean | clean |
    | honest transition delegation — the live shape, all ten of them | clean | clean |
    | the same through redundant parentheses (`(f.tracer.DraftRule)`) | clean | clean |
    | the 10 live transition delegations + all 199 live direct-call facades | clean | clean |
    | **ceiling N1** — deformed hoisted guard above a well-shaped one (recorded, not closed) | PASSED | PASSED |
    | **ceiling N2** — local shadow of the guard function name (recorded, not closed) | PASSED | PASSED |

    The two new single-property probes are the ones that make the rule's two halves separately provable: the first pins "neither a call `Fun` nor a transition argument is reported" with nothing else in the function, and the last pins "a direct element of `Args`, not merely contained inside one" with everything else about the delegation honest. The reported LINE is checked too, not just the verdict — P11 flags the handoff line, not the honest call one line below it.
  - Gates green, in order: `go build ./...`, `make test` ("All tests completed successfully"), `make check-codegen-drift` ("no codegen drift: committed clients reproduce from the specs"), `rm -rf ~/.cache/golangci-lint && make lint` (0 issues — the cold linter did not bite), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`). `git status --short` clean — both probe files are deleted, as in every prior round. Scan totals unchanged throughout: **0 parser call sites, 225 operations reached raw, 209 guarded facade functions across 205 path-parameter operations** — the stricter accounting covers the same surface rather than a smaller one. Zero behaviour change in product code: this round touched the scan test and this plan, nothing else.
  - **Method note, and it is the finding of this round.** Round 5's rule was *walk the classifier's control flow and ask, at every early exit, what this exit SKIPS for the inputs that take it* — parts compose through branches. Round 6 is one level deeper, and it is about DATA rather than control: **a set keyed by NAME collapses distinct occurrences, and every occurrence after the first inherits credit it never earned.** The union here was built from names because names are what the report printed; nothing about the check needed them. The mechanical form: **for every set a scan builds, name the key, then ask what DISTINCT occurrences that key collapses and whether any collapsed occurrence could carry its own offence.** If it could, the key is too coarse and the right key is the AST node. This generalises past sets: any aggregation — a boolean "this function delegates somewhere", a count, a name set — is a lossy key, and round 4's deleted `delegatesToTransitionHelper` was the same defect with the coarsest key of all. Three rounds, one shape: **credit must attach to the occurrence that earned it.**

- [x] **Epic 4 phase A fix round 7** (2026-08-25, post-verification). **The final fix round of the protocol.** Adversarial verification of round 6 returned FAIL: 1 HIGH — **inside a function round 5 wrote and round 6 left untouched** — plus 3 absorbable NOTES. **For the seventh round running, every finding is in the enforcement artefacts, none in the 45-site retrofit.** Two fix commits, `22ce3f4`..`1b24661`, plus this record's own commit. Not pushed. The docs debt inventory is still untouched and stays open for phase B.

  1. **HIGH, money path** — `test(entities)` (`22ce3f4`) — **a call whose argument list could not place its path positions was SKIPPED, so every path position was skipped and the function was credited.** `pathArgumentsOf` bounded each wanted position with `i >= len(call.Args) → continue`, on an assumption it never stated: that a short argument list only ever means the caller omitted the variadic request-editor tail. **Go's multi-value expansion breaks that assumption outright.** `f.ledger.GetSegmentByID(probeScope(ctx, orgID, ledgerID, id))` compiles, forwards four values through one call expression, and has `len(call.Args) == 1` — while `GetSegmentByID`'s wanted positions sit at 1, 2 and 3. All three were past the end, all three were skipped, `missing` came back empty, and the facade was **credited**. Wire-proven with `orgID = ".."`, which pops a path segment and turns a scoped read into a **cross-organization read**. Reads only: the delete-seam and idempotency scans block the destructive twin of this spelling, which is the only reason the blast radius stops at reads.
     - **An honest guard above the call does not save it.** The scan credits either way, so nothing compares the guard against anything, and a later refactor is free to delete a comparison that was never made. That is why the probe was run **both** ways and both are flagged.
     - **Class enumeration:** a call whose argument list cannot place every WANTED path position is **unreadable**, and unreadable is a **FLAG** — the same unknown=flag rule the unknown-but-generated branch six lines above already applied to a spelling with no raw `*Client` signature. The rule was in the file; it had simply never been swept across the function next to it.
     - **The bound is on the highest WANTED index, never on `len(names)`,** and that is load-bearing rather than decorative. A call that legitimately omits the request-editor tail leaves `len(call.Args) < len(names)` and is perfectly readable. **Measured, not argued:** bounding on `len(names)` instead flags **55 live facade methods**, a false-positive storm that would have buried the finding it was meant to surface.
     - The placeable arguments are still compared when a call is reported unreadable, so the fix adds a finding and never replaces one. `unguardedPathArguments` becomes a method on `pathGuardScan` purely to reach the `FileSet`: the offence names the **LINE** of the offending call, because one function can call the same operation several times and only one of those calls may be the unreadable one — the occurrence-not-symbol distinction round 6 established, applied to the report round 7 adds.
     - **One new deliberate false positive, and it is the ninth.** Anyone who writes a four-tuple bundler and spells the call `op(bundle(...))` is now refused, with its one-line fix recorded at the site: pass the arguments directly. Accepting it means reading through the inner call to the values it returns, which is dataflow, not a match. Nothing in the tree spells a call this way.
  2. **NOTES ×3, absorbed** — `docs(entities)` (`1b24661`) — no code change, three documentation debts closed.
     - **(a) The shadowing ceiling covered two name families; there are three.** A local closure shadowing a **transition helper's** name (`ruleTransition := func(...)`) is matched against `transitionHelpers` by spelling and inherits the real helper's credit: `helperGuardsWhatItForwards` checks the package-level declaration while the call site reaches the closure, so the promise the credit rests on was made by code that never runs. Guarded values, `requirePathIDs`, and the `transitionHelpers` entries are now named as one defect with one tool (identifier resolution). Verifier-confirmed, hostile-only; live exposure zero.
     - **(b) Three stale cross-references to `unknownDelegation`**, deleted in round 6, repointed to `unaccountedMentions`, where that branch's four escapes now live.
     - **(c) Parentheses around a transition helper's NAME** (`(ruleTransition)(...)`) are not read through by `calleeName`, so the call is flagged rather than credited. Consistent with what `accountedMentionNodes` already says about the argument side, but unlisted; now the **eighth** accepted false positive, with the reason it stays strict where the argument side does not: unparenthesising an ARGUMENT credits the one node that would have been credited anyway, while unparenthesising a CALLEE turns an unknown call into a transition call and credits **every element of its argument list at once**. Widening credit and widening a report are not the same risk.
  - **Deformation table, before → after** (one compiling probe file against the real generated client, deleted before the gates; "PASSED" means silently credited):

    | Probe | Before | After |
    |---|---|---|
    | **Y1** multi-value read, **no guard at all** — `GetSegmentByID(probeScope(ctx, orgID, ledgerID, id))` | PASSED | FLAGGED |
    | **Y2** the same multi-value read **with an honest four-part guard above it** (the new ninth false positive) | PASSED | FLAGGED |
    | every live write with a request-editor tail (`idempotencyEditors(ctx, …)...`) | clean | clean |
    | the live read facades — multi-argument direct spelling, all 199 of them | clean | clean |
    | the 10 live transition delegations | clean | clean |
    | **control** — the same fix bounded on `len(names)` instead of the highest wanted index | — | **55 live facades falsely flagged** |

    Y1 and Y2 both raised the guarded-facade count from **209 to 211** before the fix and left the whole suite green: the pre-fix scan did not merely miss them, it **counted them as guarded**. The reported LINE is checked too, not just the verdict — each probe flags its own call line (23 and 37), not the declaration.
  - **The sweep, and it is this round's new artefact.** Round 6's HIGH survived rounds 4, 5 and 6 because each round applied its new rule only to the code that round TOUCHED. So round 7 swept **every** skip-shaped `continue` / early `return` / `break` in all five structural scans (38 sites in the path-guard scan, 26 across the four siblings) against one mechanical question: **does skipping remove an item from a set that CREDITS (loosens — must flag, or prove emptiness at the site) or from a set that REPORTS (tightens — at worst a false positive)?** Findings:
    - **Path-guard scan: the F-A skip was the only unjustified credit-direction skip, and it is now a flag.** The other credit-direction sites are accounted for. The `len(calls) == 0` exit carries its emptiness comment (round 5's convention, re-derived in round 6). The `directPathCalls` / `calledOperationSpelling` / `bindOperations` skips are backstopped by construction since round 6: a call target the scan cannot recognise never enters `calls`, so `accountedMentionNodes` never accounts for its mention, so `unaccountedMentions` reports it — the skip converts a comparison into a flag rather than into silence. The generated-side derivation skips (`pathArgumentNames`, `clientMethodParameters`) leave `pathArgs[op]` / `methodParams[method]` empty, which is the unresolvable-spelling flag. Every remaining skip is flag-direction (`guardedValues`, the four guard-shape matchers, `contextParameter`, `functionTypedParameter`, `localStringConstants`, `calleeName`, the `forwardedValues` exclusions).
    - **One residual, recorded not closed:** a missed entry in `pathOps` itself removes an operation from the scan's universe entirely, and no flag catches that — it is bounded only by `require.NotEmpty` plus the 205-operation floor here and the 225 floor in the sibling. Same shape in `deleteOps` and `writeOps`.
    - **Two siblings still carry round 6's coarse-key defect, and this round did not touch them** (outside its mandate; flagged for phase B). `delete_seam_structural_test.go`: `callsAny(fn, "deleteResource")` is a **function-level boolean** crediting every raw delete the function names, and `rawDecides[raw[0]]` records only the FIRST exempt operation, dropping every other one the same function names. `idempotency_stamp_structural_test.go`: `stampsIdempotency(fn)` is the same function-level boolean, crediting every write the function names on one helper call anywhere in the body. Both are literally round 6's "a boolean 'this function delegates somewhere'", one scan over. **Live exposure is zero in both** — the two exempt deletes sit in separate facades with one operation each — but the shape is the one already proven exploitable next door. `input_validation_structural_test.go` is the one sibling that already satisfies the rule: it keys credit by `token.Pos`, per occurrence.
  - Gates green, in order: `go build ./...`, `make test` ("All tests completed successfully"), `make check-codegen-drift` ("no codegen drift: committed clients reproduce from the specs"), `rm -rf ~/.cache/golangci-lint && make lint` (**0 issues — the cold linter did not bite**, helped by the complexity linters being excluded on `_test.go` and by `unplaceableCallOffence` taking the message off the loop), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`). `git status --short` clean — the probe file is deleted, as in every prior round. Scan totals unchanged throughout: **0 parser call sites, 225 operations reached raw, 209 guarded facade functions across 205 path-parameter operations** — the stricter reading covers the same surface rather than a smaller one. Zero behaviour change in product code: this round touched the scan test and this plan, nothing else.
  - **Method note, and it is the finding of this round.** Rounds 4, 5 and 6 each installed a rule — *unknown is a flag*; *an early exit must prove what it skips is empty*; *credit attaches to the occurrence, never to the symbol*. Round 6's HIGH violated the first two and survived all three, for one reason: **each rule was applied to the code that round happened to be touching, and never swept across the stock.** `pathArgumentsOf` sat six lines below a branch that already said "unknown-but-generated is therefore reported" and did the opposite, for three rounds, unread. The mechanical form: **when a round establishes a rule, grep the whole scan for the pattern that rule bans — in the same round.** `continue`/`return` that skips instead of flagging; a set keyed coarser than the node; a branch entered on partial evidence. **A rule that only governs new code is a linter nobody ran on the existing code**, and the existing code is where three rounds of previous fixes are sitting. Round 7 ran that sweep, and it is the artefact that should outlive the round: the rule and the sweep ship together, or the rule is decoration.

- [x] **Epic 4 phase A fix round 8** (2026-08-25, post-sweep). **Not a verification round: this round is round 7's sweep being FINISHED.** Round 7 swept all five structural scans, found the two siblings carrying round 6's coarse-key defect, and left them "flagged for phase B". Round 8 applies round 6's proven per-occurrence recipe to both. Two fix commits, `2bb6fb7`..`a122a9d`, plus this record's own commit. Not pushed. Scope was the two named defects and nothing else. The docs debt inventory is still untouched and stays open for phase B.

  1. **`delete_seam_structural_test.go`** (`2bb6fb7`) — **`callsAny(fn, "deleteResource")` was a function-level boolean, and `rawDecides[raw[0]]` recorded only the first exempt operation.** A facade routing one delete through the seam honestly and naming a second raw delete that never reached it was credited for both.
     - **The accounting change.** `deleteOperationsNamedBy` returned `(parsed, raw []string)` off two name-keyed sets; it is now `deleteMentions` returning `(parsed, raw []*ast.SelectorExpr)` — one entry per OCCURRENCE. The old switch is gone; each raw mention is judged on its own by `rawMentionOffence`, in three steps: the mention must be the **callee of a call** (`calleeCallOf`), an operation on the inline-decision list must hand **that call's** result to `isSuccess`, and every other delete call must hand its result to `deleteResource`. Credit is `s.seamed++` per accounted CALL, and `s.rawDecides[op] = true` is keyed **per operation**, set only when that operation's own call was verified. `allStatusDeciding` and `decidesOnStatus` are deleted with the branch they served: the mixing case they defended against ("a function mixing an exempt delete with a real one is NOT exempt") existed only because credit was function-level, and per-occurrence judgment subsumes it.
     - **The core lines.** Before: `case allStatusDeciding(raw): rawDecides[raw[0]] = decidesOnStatus(fn); seamed++` / `case !callsAny(fn, func(n string) bool { return n == "deleteResource" }):`. After: `if _, exempt := statusDecidingDeletes[op]; exempt { if !resultsReach(fn, call, "isSuccess", locals) { return …offence… }; s.rawDecides[op] = true; s.seamed++; return "" }` / `if !resultsReach(fn, call, "deleteResource", locals) { return …offence… }`.
     - **An exempt operation is deliberately NOT also accepted through `deleteResource`.** Routing one through the shared seam is the right change and it comes with deleting its entry from `statusDecidingDeletes`; accepting both would make the exemption unfalsifiable.
  2. **`idempotency_stamp_structural_test.go`** (`a122a9d`) — **`stampsIdempotency(fn)` was the identical function-level boolean.** One stamped write credited every write the function named.
     - **The accounting change.** `writeOperationsNamedBy` (name-keyed) becomes `writeMentions` (node-per-occurrence); `classifyWrite`, `allExempt` and `stampsIdempotency` are deleted and replaced by `idempotencyStampScan.offences`, which judges each mention: an **exempt operation is skipped wherever it is named** (the exemption is a statement about the OPERATION, which is what lets the rule and reservation transitions hand it to a helper as a function value), every other mention must be the **callee of a call**, and that call must carry a key.
     - **The core lines.** Before: `if stampsIdempotency(fn) { return "", true }` — `callsAny(fn, …idempotencyHelpers[name] || stampPreparers[name]…)`. After: `case !carriesStamp(call, carriers): …offence…` / `default: s.stamped++`, where `carriesStamp` accepts a **stamp call that is a direct argument of this write call**, or **a body variable this write call names that a stamp call touched** (`stampCarriers`). Both directions of the carrier are needed because the two live shapes point opposite ways: `applyIdempotency(&params.XIdempotency, …)` writes the key THROUGH its argument, `scoped, params, err := prepareCreate(…)` returns it.
     - The `preparerResolves` check is deliberately left function-level and now says so in the comment: "does this helper resolve a key anywhere in its body" is the right question for a helper that exists to do nothing else.
  - **The linkage is source-structural, and both scans now state what it cannot see.** There is no type information and no dataflow. `resultsReach` matches an assignment's left-hand names against the identifiers a seam call mentions; `carriesStamp` matches the identifiers a stamp call touches against the identifiers a write call mentions. Neither can prove the matched variable is the one carrying the response or the key. **`bodyVariables` is what makes the match mean anything**: parameters and the receiver are excluded by construction (every call in a function shares them, so `ctx` on both sides is evidence of nothing), and body **constants** are excluded one step further, so the `const operation = "Assets.Delete"` every facade declares cannot link a call to anything. Residuals, recorded not closed, both in the loosening direction: a stamp call's arguments credit every body variable they name (live, the only ones are the params structs the stamp writes into — the intended link), and an assignment from a stamp preparer credits every variable on its left, `err` included (no generated client method takes an error, so nothing live can be credited through that name). The tightening-direction cost is a false positive on an honest delete or write written some third way — through a struct field, a channel, a local wrapper — whose fix is to write it the way the other 29 / 109 are.
  - **Deformation table.** Each probe is a compiling facade written against the real generated clients, in a non-test file so the scans see it, run against the OLD code and the NEW code, then deleted. **Every one of the seven PASSED under the boolean.**

    | # | Deformation | Old | New |
    |---|---|---|---|
    | A | one honest seamed `DeleteAsset` + a second raw `DeleteAccount` that never reaches `deleteResource` | PASS | FLAG — "reaches DeleteAccount on line 16 without deleteResource" |
    | B | exempt `DeleteRule` whose call result never reaches `isSuccess`, beside an `isSuccess` call on an unrelated value | PASS | FLAG — "…but that call's result never reaches isSuccess" |
    | C | `DeleteAsset` hoisted into a local, then called through the local | PASS | FLAG — "names DeleteAsset on line 50 without calling it there" |
    | D | two exempt deletes, only the FIRST decided (`rawDecides[raw[0]]` dropped the second) | PASS | FLAG — "reaches DeleteLimit on line 37 … never reaches isSuccess" |
    | E | one stamped `CreateAsset` + a SECOND mention of the SAME operation, unstamped | PASS | FLAG — "writes via CreateAsset on line 61 without an idempotency key" |
    | F | params-struct shape: one write carrying stamped `params` + a second carrying its own unstamped params | PASS | FLAG — "writes via CreateTransactionInflow on line 74 without an idempotency key" |
    | G | `CreateAssetWithBody` hoisted into a local, then called through the local | PASS | FLAG — "names CreateAsset on line 81 without calling it there" |

    Cross-check worth keeping: the delete probes A–D also trip the **idempotency** scan, because a DELETE is a write, and the deformed deletes carry no editors. Every live facade stays clean in both scans with the probe removed.
  - **The universe-derivation answer, which round 7 left as an open question.** All three universes are **DERIVED, not hand-maintained.** `deleteOperations` and `writeOperations` parse `../internal/genledger` and `../internal/gentracer` and keep every request builder whose body issues DELETE (respectively POST/PUT/PATCH/DELETE); `operationsWithPathParameters` keeps every builder that formats a value with `runtime.ParamLocationPath`. So the 29 / 122 / 205 counts are computed each run and the floors are a backstop against the **scan** breaking, not against a missed entry — round 7's "a missed entry in `pathOps` itself" residual is narrower than it read. What survives is the **reading**, and only for the two method-based universes: the HTTP method is matched in the builder's SOURCE TEXT as a literal `http.NewRequest("DELETE"`, so a generated builder that computed its method would drop out silently. Measured: all **225** `http.NewRequest` calls across both generated clients spell it as a bare literal (89 GET, 60 POST, 32 PATCH, 29 DELETE, 14 HEAD, 1 PUT — and 60+32+29+1 = the 122 write operations the scan derives, exactly). The residual is **currently empty** and reopens only if oapi-codegen changes how it emits the call. Both doc comments now carry this paragraph.
  - Gates green, in order: `go build ./...`, `make test` ("All tests completed successfully"), `make check-codegen-drift` ("no codegen drift: committed clients reproduce from the specs"), `rm -rf ~/.cache/golangci-lint && make lint` (**0 issues**, after splitting `bodyVariables` and `stampCarriers` into one helper per direction — the cold linter bit both on `cognitive-complexity: 23 (> max enabled 20)` from the type-switch inside an `ast.Inspect` closure, and the split reads better than the switch did), `scripts/check-config-parity.sh` (16 config Options), `go mod tidy` (no-op on both `go.mod` and `go.sum`). `git status --short` clean — the probe file is deleted, as in every prior round. Scan totals unchanged throughout: **0 offenders in all five scans; 29 → 29 accounted delete call sites; 109 → 109 accounted write call sites across 122 write operations; 209 guarded facade functions across 205 path-parameter operations; 225 operations reached raw.** The two floors now count CALL SITES rather than functions — the same number while every facade names one operation, and the honest number the moment one names two. Zero behaviour change in product code: this round touched two scan tests and this plan, nothing else.
  - **Method note, round 8's addendum to round 7's rule.** Round 7 established *a rule and its sweep ship in the same round*. What it did NOT establish is what a sweep owes: it found the two siblings, wrote them down, and marked them "flagged for phase B" — and phase B is where findings go to be rediscovered. **The sweep's output is WORK, not a memo.** A sweep that ends in "flagged for the orchestrator" has moved the decoration one desk over: the finding is now a line in a plan instead of a defect in a scan, and nothing about the money path changed. The mechanical form: **a sweep is done when every instance it found is fixed, or ceilinged with a stated reason in the code that carries the ceiling.** "Outside this round's mandate" is not a reason — the sweep IS the mandate the moment it returns a hit, and round 8 existing at all is the cost of having deferred two three-line-shaped fixes. Note the size: both defects were **one accounting model**, already written and proven next door in round 6; the expensive part was never the fix, it was the second round.

- [x] **Epic 4 phase A — CLOSED** (2026-08-25, orchestrator final review). The 45-site retrofit plus eight fix rounds of structural hardening, `8e4d7a3`..`8593d47`, not pushed. **The product surface never had a finding: all eight rounds' HIGHs were in the enforcement artefacts — the structural scans — and the 45-site retrofit was verified clean by exhaustive diff in round 1 and never re-opened.** Live totals immobile throughout: 0 generated-parser call sites, 225 operations reached raw, 209 guarded facade functions across 205 path-parameter operations, 29 accounted delete call sites, 109 accounted write call sites across 122 write operations.
  - **Orchestrator's independent spot-checks, run directly, not delegated:** (1) read round 7's `pathArgumentsOf` fix in place — unplaceable wanted position sets `placed = false` and the caller at :1005-1006 reports it; (2) re-applied round 6's Y1 deformation (multi-value expansion `GetSegmentByID(bundle(ctx, orgID, ledgerID, id))`) by hand — FLAGGED, and the offence message explains the mechanism; (3) applied a fresh round-8 deformation by hand (one honest seamed `DeleteAsset` + a second raw `DeleteAsset` in the same function) — FLAGGED at the second call's line while the honest call stayed credited, which is per-occurrence accounting doing exactly what it claims; (4) probes deleted, `go build ./...` + `go test ./entities/` green on the clean tree.
  - **Why eight rounds for meta-tests, and why it stopped here.** The scans guard the "`..` pops a path segment" scope escalation on a money path — a raw DELETE reaching the wire with a caller-controlled `..` deletes the PARENT resource — so each proven escape was worth a round. The arc converged: rounds 2-7 each closed a strictly finer accounting defect (boolean per function → set per name → credit per AST occurrence), and per-occurrence is the finest granularity the AST has, so the collapse-of-key class is exhausted by construction. What remains open is enumerated, not vague: five ceiling entries (reassignment, shadowing of three spelling-matched name families, hoisted guard value, request-editor path rewrite, reflection) that all need identifier resolution or dataflow — a different tool, honestly named in the scan — and nine deliberate false positives, each recorded with its one-line fix.
  - Phase B (docs debt inventory + examples + `make ci`/`make verify-sdk`) is dispatched next; then integration against Midaz develop.

- [x] **Epic 4 phase B — examples + docs debt inventory** (2026-08-25). Three commits, `59acf43`..`0f9a389`, not pushed. Closes the docs debt inventory. **One gate fails and it is NOT phase B's doing — see the coverage entry below; it blocks the PR and needs a decision.**
  1. **The examples taught the deprecated surface exclusively** — `docs(examples)` (`59acf43`). Measured before touching anything: **70 `.V1.` call sites across 19 example directories, and ZERO `.V2.`**. Epic 2 renamed the flat accessors to `c.V1.*` to restore compilation, which was right then and left every example pointing at the surface Midaz deprecated. The CRUD families moved to `c.V2.*`; for those the two surfaces are the same method names over the same models, so the swap is compiler-verified rather than asserted.
     - **Two examples shipped DEFECTS, not staleness, and both would fail on first run.** `04-listing-cursor` set `Filters.Status` and `Filters.AssetCode` — two of the six filters Epic 3 fix round 2 made a local refusal — so the example as shipped errors out before sending a request, while teaching the reader to narrow by a field that never worked. Replaced with what the endpoint honours (a date range, the metadata pair) and given a fifth demo, `Count`, since that is the one place `Status` and `Route` still narrow; its doc names the TODAY default window. `05-listing-pages/README.md` listed `AssetRates` and `Holders` as page-based endpoints; both embed `CursorListOpts`, so a reader following it would not compile.
     - `03-end-to-end` moved to the /v2 creation path (`CreateDirect`, flat legs), which is **shorter** than the /v1 nested `send` envelope it replaced. Its README also claimed to walk organization → ledger → asset → account; the example only ever created a transaction, against hard-coded ids.
     - **The workflow transaction flows stay on `c.V1` deliberately** (12 `CreateJSON` sites across 4 files) and now say why in each file header: the nested send/source/distribute creation style exists ONLY on /v1, so there is no /v2 twin to swap onto. Rewriting them onto `CreateDirect` was rejected — it would ship unverified money-path example code, since `make demo-data` needs a live server and is the integration phase's job, not phase B's.
     - Example READMEs named accessors and methods that do not exist: `c.Organizations.ListOrganizations`, `c.Organizations.CreateOrganization`, `ListAccountsPages`, `ListTransactionsAll`.
  2. **The two API maps described the pre-v5 world** — `docs(mapping)` (`bfe235d`). `external_apis.md` listed **16 flat ledger accessors**, none of which exist, and documented `BalancesService` / `OperationsService` / `AliasesService` as live services. It also gave the alias and external-code balance lookups an `opts` parameter and four `All`/`Pages` iterators that Epic 2 fix round 1 deleted, listed `BalancesFilters` and `AliasesListOpts` as opts types (neither exists), claimed `PageListOpts` covers Balances and Holders (both cursor), and carried `WithCRMURL` / `config.WithCRMURL` / `MIDAZ_CRM_URL` while **omitting `MIDAZ_TRACER_API_KEY`**. `internal_apis.md` claimed three service URL keys (there are two), described the interface-backed trio as live, and wrote every path group **without its version prefix** — which is now the entire versioning mechanism.
     - The `ToQueryParams` paragraph the Epic 3 close-out corrected was re-verified against the code and **kept**: 14 types carry it, and the four named as lacking it (`BalancesListOpts`, `OperationsListOpts`, `AccountOperationsListOpts`, `TransactionsListOpts`) genuinely do not.
     - The transaction path group is now spelled out per surface rather than prefixed generically, because the two differ in SHAPE: /v1 creates are ledger-scoped, /v2 creates are top-level and carry no scope in the URL at all.
  3. **The guides, and the generated godoc, which nobody had regenerated since Epic 1** — `docs` (`0f9a389`). `README.md` still opened "the idiomatic **v4** client" with a "What's new in v4" section; it now states v5 and its four breaking changes with the reason each exists. Its service-access section framed `c.V2` as a nine-service add-on to `c.V1` — **backwards**, since V2 is the wider surface (22 against 14) and the one to build against. Every Go block in `docs/examples.md` used a flat accessor, so none compiled, and `CalculateBilling` was missing its ledger argument (the billing family moved to ledger scope in /v2). `pagination.md` mis-classified Balances as page-based — load-bearing, since that shape is exactly the unbounded-loop defect Epic 2 fixed — and claimed a uniform `List`/`All`/`Pages` trio across accessors that use prefixed spellings (`AssetRates`, `Instruments`).
     - **`midaz.go`'s own package doc was a source of the drift**, not just a victim: it taught `c.V1.Organizations.Get` as the quickstart, named the pagination trio `ListAll`/`ListPages` (not the method names), and claimed `examples/03-end-to-end` walks the resource hierarchy. Fixed at the source, then `make docs` re-run.
     - `docs/godoc/**` regenerated. It had drifted since Epic 1: still listing `Alias`, `AliasesListOpts`, `AliasesFilters`, `BalancesFilters`, `CreateAliasInput` and `UpdateAliasInput`, missing the entire V2 model set (`TransactionV2`, `OperationV2`, `CreateTransactionV2Input`, `TransactionV2Leg`, `TransactionV2Share`, `UpdateTransactionV2Input`), and still showing the pre-rename tracer signature `NewCreateLimitInput(..., currency string)`. The dumps ARE tracked (18 files) and regenerated historically (`eaf10ad`), notwithstanding the `/docs/godoc` line in `.gitignore` — so they are committed.
  4. **`docs/plans/transaction-metadata-list-filter.md` DELETED**, not marked superseded. It is a *"Draft for review"* whose entire subject — exposing a transaction metadata list filter — **shipped**: `TransactionsFilters.MetadataKey`/`MetadataValue` exist and work. Every file and line it cites is gone (`entities/transactions.go`, `models/transactions_list_opts.go:31-50`, `ToQueryParams:61-89`). Its surviving value — the analysis of why the other six filters cannot work, and the three dead ends (`ExternalID` is `json:"-"` and inert, `reference`/`code` do not pair, the idempotency key is not a read handle) — is carried by the `TransactionsFilters` doc comment with the handler evidence, and by this plan. Keeping a superseded draft invites someone to implement it a second time.
  - **Env/config parity: PASS, no drift.** `scripts/check-config-parity.sh` verifies 16 config Options. The three `.env*.example` files agree with `pkg/config/config.go:FromEnvironment` and with each other; `MIDAZ_TRACER_API_KEY` is present in all three (as a documented commented-out line). This branch touched them exactly once, in `ad3e193`, to remove the `/v1` pin from `MIDAZ_LEDGER_URL` and explain why the two planes' base URLs differ in shape. Nothing to fix.
  - **Gates, run in order, verbatim.** `go build ./...` PASS. `make test` — "All tests completed successfully". `make check-codegen-drift` — "no codegen drift: committed clients reproduce from the specs". Cold lint (`rm -rf ~/.cache/golangci-lint && make lint`) — **0 issues**, first try, no style round needed: phase B changed no test code, which is where the previous four rounds' cold-lint bites all lived. `scripts/check-config-parity.sh` — 16 config Options. `make verify-sdk` — **"All SDK quality checks passed!"**. `make gosec` standalone — PASS. `go mod tidy` no-op on both `go.mod` and `go.sum`; `git status --short` empty.
  - **`make ci` FAILS, at `coverage`, and it is a PRE-EXISTING regression this branch introduced in Epics 1-3 — not phase B.** The pipeline is `tidy fmt lint gosec test test-contract coverage verify-sdk`; everything through `test-contract` passed, `coverage` aborted the run, and `verify-sdk` therefore never executed inside `make ci` (it passes standalone). **Total coverage 79.4% against a 80.0% threshold.**
    - **Measured on three trees rather than assumed.** develop @ `4143472`: **81.2% — PASSES**. Epic 4 phase A HEAD @ `3ae0fe8`: **79.4%**. Phase B HEAD @ `0f9a389`: **79.4%, identical**. So phase B moved it by exactly zero, which is expected — the coverage target excludes `examples`, `mocks` and `internal/gen`, and phase B's only non-example, non-doc change was doc comments in `midaz.go`.
    - **The whole drop is in ONE package**: `entities` **76.4% → 72.4% (−4.0)**, measured by aggregating the two coverage profiles per package. Every other package is within ±0.5. `entities` is the largest package, so −4.0 there is −1.8 on the total.
    - **The cause is the epic's own test strategy, and it is a real tension rather than an oversight.** Epics 2-4A deliberately replaced per-method behavioural tables with **structural scans** — the method note recorded four times in this plan ("derive the expectation from the generated code; sample plus scan is what covers every site"). A scan proves an invariant across all 225 operations by parsing the AST, and executes almost none of the facade statements it protects; the per-method tables it replaced executed many. Meanwhile Epic 3 added 13 new V2 facades — a large body of new statements — whose pin is a seven-row sample plus a scan, by explicit design ("a behavioural row per site would be forty-five servers proving one helper forty-five times"). So the surface is guarded **more** strongly than before and scores **lower** on statement coverage, and the 80.0% gate cannot see the difference.
    - **Not fixed here, and deliberately not.** Two honest routes and both are decisions above phase B's mandate: (a) add behavioural tests over the V2 facades to lift `entities` back over ~76% — real work, roughly a fix round, and a judgment call about where test investment goes; or (b) argue the threshold down on the grounds that mutation-proven structural scans beat the statements they skip — which is lowering a quality gate on a money-path SDK, a product decision. Writing tests chosen to move a percentage rather than to pin a behaviour is precisely the sampling anti-pattern this epic has fixed five times. **This blocks the PR: `make ci` is green on develop and red on this branch.**
  - **RESOLVED by the phase B coverage fix round below**, via route (a): behavioural tables over the V2 facades. Route (b) — lowering the threshold — was refused by orchestrator decision and never attempted.
  - **Flagged, found but deliberately NOT changed.** (1) **The `V1` service count in the phase-B briefing was wrong** — it said 17; `entities/version_groups.go` declares **14**, and V2 declares 22. Everything written was counted from the struct, not copied. (2) **`workflow-with-entities` covers none of the nine V2-only services** — Holders, Instruments, Encryption, Composition, ProtectionAudit and the four billing accessors have no runnable example anywhere, so its README claim "Every public service through full CRUD" is now false by nine. Writing nine new workflows is a feature, not a docs pass. (3) **`docs/architecture.md` is marked historical** at the top and describes the pre-v3 `mmodel` conversion layer; only its entity-layer bullet was corrected, the rest was left as the file's own header instructs. (4) The `onboarding` service-key rename candidate (→ `ledger`) is still open, recorded in the docs rather than acted on.

- [x] **Epic 4 phase B coverage fix round** (2026-08-25). Four commits, `5f247e7`..`c652a8d`, not pushed. **`make ci` now passes END TO END, `verify-sdk` included.** Zero product-code change: `git diff` against the phase-B HEAD touches only new `*_test.go` files.
  - **Orchestrator decision, taken before any code: write the behaviours, do not touch the threshold.** The scans prove invariants; behavioural tables pin outcomes; a money-path SDK needs both. The 13 V2 facades had been pinned by "sample plus scan" since Epic 3 with the per-method tables explicitly deferred — this round is that deferral being paid, not a percentage being chased.
  - **The arithmetic, measured on the profiles rather than asserted.** Total **79.43% → 81.52%** against the 80.0% threshold (12 068 → 12 386 covered statements of 15 193). The whole movement is in one package: `entities` **72.30% → 80.19% (+7.9)**; every other package is unchanged, because nothing outside `entities/` was touched. The branch is now **above develop** (81.2%), which is where it was before Epics 1-3.
  - **What the four tables pin, and why each behaviour is one a caller depends on.**
    1. `v2_list_wire_test.go` — **every narrowing a caller sets on a V2 list reaches the wire.** The V2 list params are BRIDGED from their V1 twins field by field (oapi-codegen emits a distinct type per operation), and a field missed in that copy is the quietest defect the SDK can ship: the request goes out unnarrowed and the full result set comes back reading exactly like a narrowed one. Twelve rows, page and cursor families, each asserting the complete query the row's opts can express plus the page it decodes back. A negative row pins the other direction — unset opts must OMIT the parameter, never send `limit=0`, which a server honouring it turns into "no results".
    2. `v2_write_wire_test.go` — **both ends of all 39 V2 write call sites.** A write is silently wrong in two ways: the request loses a field, so the ledger creates a resource the caller did not describe; or the response does, so the caller books an id and status that read as zero values with a nil error. Each row states the method and body fragments that must leave AND the response fields that must arrive. Idempotency is asserted where it protects the caller — the header on the wire — auto-stamped with the gate on, and the caller's explicit key winning with the gate off.
    3. `v2_read_wire_test.go` — **the reads whose shape is their own**: ledger settings (all-boolean, where a dropped gate is indistinguishable from a deliberate `false` and every one reads "permitted" when lost — so the fixture sets each to the non-zero value), balance history (exact decimals, because it is what a reconciliation run compares against), the two alias/external-code account lookups, the two by-alias balance lists, the bare-array metadata-index list, and the V2 counts. The counts get both halves: `X-Total-Count` is read, and an absent / blank / non-numeric / negative total is REFUSED — a silent zero reads as "there is nothing here", the same answer as an empty ledger, and is why a caller stops looking.
    4. `v2_refusal_test.go` — **one table of 35 V2 call sites, two refusals, at the two ends of a call.** Outbound: a dangerous path id (empty, whitespace, `.`, `..`, a separator, a percent-encoded dot segment) is refused before a request exists, and **the counting server is the assertion** — an error alone also comes back from a 404, and a 404 means the request was SENT, which for a `..` delete means it was sent one scope up. Inbound: a gateway 403/404 with an empty body keeps its status and never becomes an SDK-internal 500.
  - **Mutation-proven, not merely green.** Every table was checked against a real defect rather than trusted because it passed first run: deleting `Alias` from `listAccountsV2Params` reddens exactly the accounts list row; deleting the `requirePathIDs` guard from `segmentsV2Facade.Delete` reddens exactly that row's five dangerous-id subtests on the zero-request assertion; swapping `readCount`'s non-2xx branch from `DecodeProblemJSON` to `NewInternalError` reddens all six count rows on both statuses. All three product-code mutations were reverted, and `git diff --stat` is empty.
  - **Cold lint bit four times, and every fix was structural rather than a suppression.** `nilnil` on a `(map, error)` helper returning `nil, nil` — changed to return an EMPTY field set, so a nil-transaction-with-nil-error (itself the defect the rows hunt) fails naming the missing field instead of panicking on a dereference; an unused `//nolint:errcheck` directive removed; two `unparam` hits on fixture helpers whose `limit` was always 5 — the parameters were dropped for named constants, which also keeps each row's `wantQuery` a LITERAL expectation rather than one computed from the same fixture it sent.
  - **Gates, run in order, verbatim.** `go build ./...` PASS. `make test` — "All tests completed successfully". `make check-codegen-drift` — "no codegen drift: committed clients reproduce from the specs". Cold lint (`rm -rf ~/.cache/golangci-lint && make lint`) — **0 issues** on a genuinely cold cache, after the four fixes above. `scripts/check-config-parity.sh` — 16 config Options. **`make ci` — PASS end to end**, coverage reporting `Total coverage: 81.5% (threshold: 80.0%)` and `verify-sdk` executing for the first time inside the pipeline: "All SDK quality checks passed!". `go mod tidy` no-op; `git status --short` empty.
  - **One surprise, and it is not a bug.** The V2 facades' immunity to the gateway-status defect is a property of HOW they are written — they read raw responses instead of the generated status-exact parser — which is precisely the kind of guarantee that survives until someone rewrites one method. Before this round nothing tested it on the V2 side at all: the `bareArrayReads` table covers five bare-array sites and `listReads` eight list paths, and neither touches `readOne`, `writeJSON`, `deleteResource` or `readCount`. `readCount` in particular had **no** status-preservation coverage on either surface — the existing V1 count-error test asserts only `err != nil`, so the mutation above would have left the whole suite green before this round. Construction-immunity now has a test.
  - **Method note.** Phase B's own record framed the choice as "(a) real work, roughly a fix round" versus "(b) lower the gate", and the estimate was right on size and wrong on what the work WAS. None of the four tables was written to move a number; the number moved 7.9 points in `entities` as a by-product of enumerating call sites that had never been executed by a test — 35 guarded paths, 39 writes, 12 list bridges, 6 counts (the seventh count was missed; see fix round 2). The generalisable form: **when a coverage gate fails on a surface that a structural scan already guards, the gap is not "untested code", it is code whose OUTCOMES were never stated.** A scan proves the same thing about every site and executes none of them; that is a virtue when the property is an invariant and a blind spot when the property is a value. The threshold was never the question — it was the messenger.

- [x] **Epic 4 phase B fix round 2** (2026-08-25, post-review). The production review of phase B returned FAIL: 5 HIGH, 3 MEDIUM, 6 LOW. **Every finding was in public-facing material — docs, examples, test tables, this plan's own numbers — and none in product code.** Four fix commits, `60ef0fa`..`61247e0` plus this record's own commit, not pushed. The only product-code line touched in the whole round is one doc comment (L14 below), which was the single product change the round's mandate permitted.
  - **What the docs claimed that the code refuses.** `docs/mapping/external_apis.md` documented `models.NewCreateAliasInput` and `models.NewUpdateAliasInput` as live builders 240 lines after the same file records that Midaz renamed the resource to instruments and that the alias models went with it — deleted. `docs/examples.md` taught three things that fail on first run: `c.Balances` (a flat accessor Epic 2 deleted; the same file spells `c.V2.Balances` correctly three times), `GetBalanceHistory(..., "2026-06-30")` — a date-only value the facade refuses **on purpose**, since a point-in-time read names an instant and `balanceHistoryDateLayouts` omits the date-only layout deliberately — and `MetadataIndexes.ListMetadataIndexes`, which is spelled `List` on both facades. It also credited `09-testing-with-mocks` to `go.uber.org/mock`; that example uses a hand-written double and says so in its own package doc.
  - **`examples/workflow-with-entities/README.md` was wrong in both directions at once.** It named three flat accessors Epic 2 deleted (`c.Organizations`, `c.Ledgers`, `c.Transactions`) AND claimed to walk "every public resource in the SDK" — false by nine, which phase B's own record already flagged and left. The intro now carries the `c.V1.*` / `c.V2.*` spellings, says it covers the dual-served families, says the transaction flows stay on `c.V1.Transactions` because the nested send/distribute style exists only on /v1, and names the nine V2-only services it does **not** walk.
  - **The list table broke its own exhaustiveness contract, and the sweep found more than the review did.** `v2_list_wire_test.go`'s header promised "every narrowing the row's opts can express — exhaustive, not a sample"; two rows were samples. Accounts set **5 of the 12** fields `AccountsFilters` declares, Operations **2 of 4**. Both are now exhaustive — every field set, every resulting query parameter asserted — and the negative row's omitted-key list was widened to the same 17 keys, so "unset must OMIT" is checked for all of them rather than eight.
  - **The proof, run to completion twice with identical verdicts.** For each of the **102** `Field: v1.Field` assignments in `entities/v2_params.go`: delete the line, `go test ./entities/ -count=1 -timeout 90s`, restore. **90 redden. 12 survive, and all twelve are vacuous by construction** — they copy a field NO opts type can set, so they always copy nil:
    - **`Metadata`, on all nine bridge functions that carry it.** No V1 mapper in the package assigns `params.Metadata` at all (verified by grep across `entities/`, not by eye) — the ledger's JSON-string metadata predicate has no SDK opts surface. The transaction list's `metadata.<key>` predicate is a different query parameter, injected by `metadataFilterEditors` and pinned in `transactions_list_filters_test.go`.
    - **`DoingBusinessAs` and `LegalDocument`, on `listOrganizationsV2Params`.** `OrganizationsFilters` declares neither and `listOrganizationsParams` never sets them; the two identifiers appear nowhere else in the package.
    - **`Cursor`, on `listAccountTypesV2Params`.** `AccountTypesListOpts` embeds `PageListOpts`, so there is no cursor to copy.
  - **The twelve stay in the bridge, and the ceiling is written where the claim lives.** Deleting them would be the silent drop the bridge exists to prevent, arriving later: the whole point is that a filter added to a V1 mapper reaches /v2 without anyone wiring it twice. So the list table's header now enumerates all twelve with the reason each is unreachable, and states that a **thirteenth** unreddenable line is a finding rather than an exemption. That is phase A's round-8 rule applied without a second round: the sweep's output is work, and the ceiling carries its reason in the code.
  - **The `-timeout 90s` on every sweep run is load-bearing, not hygiene.** The reviewer's own sweep stalled indefinitely: deleting a cursor field makes a pagination iterator re-request the first page forever, and `t.Fatalf` from inside an `httptest` handler goroutine does not stop the test goroutine that is waiting on it. A run that times out counts as REDDENED — a hang IS the defect that mutation introduces.
  - **The count table said "one row per V2 count" over six of seven.** `V2.Transactions.Count` was missing — the only count taking opts, which is why it was the one left out. It shares `readCount`, so the row is one entry. Its refusal half also asserted only `err != nil`; the server answers **200** there, so the request and the caller's permissions are fine and the SDK could not read the reply. The row now asserts the SDK-internal category, matching how its sibling in `v2_refusal_test.go` asserts the validation category. An unreadable total surfacing as a validation or not-found error would send a caller to fix an input that is correct.
  - **Table headers that opened wider than the table.** `v2_refusal_test.go` and `v2_write_wire_test.go` both began "one row per V2 call site" and narrowed to thirteen facades two lines later. The scope is now in the first line, and both name the nine V2-only facades' own per-facade tests (all nine exist — `holders_facade_test.go` through `audit_facade_test.go`, verified rather than assumed). `v2_list_wire_test.go`'s header gained the same treatment plus the reason the transaction list has no row here: it refuses six of its filters and carries its metadata predicate through a request editor, so it is pinned end to end in `transactions_list_filters_test.go` instead of duplicated.
  - **This plan's own numbers were wrong twice.** "36 V2 write call sites" — the table has **39**, counted from it. "the six V2 counts" — there are **seven** once the missing row exists. Both corrected in place, and the second correction is the finding, since the sentence was true only because the row was absent.
  - **Both API maps omitted the insecure-HTTP knob** that `docs/configuration.md` and `docs/environment.md` both carry: `midaz.WithAllowInsecureHTTP`, `config.WithAllowInsecureHTTP`, `MIDAZ_ALLOW_INSECURE_HTTP`. Added to both, with the two things a reader needs and neither map said — it must be applied BEFORE the URL setters (each URL is validated as it is set; `FromEnvironment` orders it correctly on the caller's behalf), and `Validate` refuses it together with `EnvironmentProduction` (`pkg/config/config.go:1309`). The internal map's env list was missing `MIDAZ_ACCESS_MANAGER_ALLOW_INSECURE_HTTP` as well — same defect, one sibling over, fixed in the same pass rather than flagged. `external_apis.md` was also missing `Accounts.GetByExternalCode`, which reaches an asset's EXTERNAL account — the counterparty every deposit is drawn from — and exists on both accounts facades.
  - **~40 comment and README lines across the examples labelled CURRENT behaviour with a stale SDK generation** — "the v3 idiom", "the v3 retry surface", "the idiomatic v4 pattern", "v3 requires exactly one auth source". The generation carried no information in any of them: the surface is simply what it is, and the invariant will outlive v5 too. So the label is **gone** rather than bumped, which is also why the count is ~40 and not the reviewer's ~30 — the grep sweep found more than his list, and every one was the same defect. Where a line genuinely dates a removal (`WithObservability(t,m,l bool)`) the version stays, reworded so it reads as history rather than as a label. Historical statements in the two API maps ("removed in v4.2", "added in the v4 remodel") were left alone; they are dates, and correct.
  - **One "v2" meant the SDK generation in a file where V2 names the ledger surface.** `04-listing-cursor/main.go`'s package doc said the type system "replaces v2's silent-drop footgun" in a file whose every call is `c.V2.*`. It now says SDK v2 and says which one it means. The same ambiguity in `external_apis.md`'s pagination paragraph was fixed the same way.
  - **`pkg/transaction/settlement.go`** — the only product-code line in the round. `balanceReader`'s doc told callers to pass `c.V1.Balances`; both facades satisfy it structurally, and the deprecated surface is the wrong one to teach.
  - **Gates, run in order, verbatim.** `go build ./...` PASS. `make test` — "All tests completed successfully". `make check-codegen-drift` — "no codegen drift: committed clients reproduce from the specs". Cold lint (`rm -rf ~/.cache/golangci-lint && make lint`) — **0 issues**. `scripts/check-config-parity.sh` — 16 config Options. **`make ci` — PASS end to end**, coverage `Total coverage: 81.5% (threshold: 80.0%)`, `verify-sdk` reporting "All SDK quality checks passed!". `go mod tidy` no-op; `git status --short` empty.
  - **Method note, and it is the same one phase A learned three times.** Nineteen of the twenty findings were in material that no compiler, no linter and no test reads: prose that names an accessor, a method, an argument or a version. The two API maps and the guides are the SDK's actual public interface for anyone who has not read the code, and they had drifted in exactly the places a reader trusts most — a builder that does not exist, an argument shape the facade refuses **on purpose**, a scope claim ("every public resource") that a reader cannot check without counting the struct. **The generalisable form: a docs pass that fixes what changed is not the same as a docs pass that verifies what is claimed.** Phase B corrected everything Epics 1-3 broke and left standing every claim that had been wrong all along; this round is the difference. The one finding that was NOT prose — the list table's two sampled rows — has the same shape one level down: a header asserting exhaustiveness that nothing checked, which is a claim, not a test, until a sweep makes it one.

- [x] **Epic 4 — CLOSED** (2026-08-25, orchestrator final review). Phase A (45-site retrofit + 8 structural-hardening rounds) and phase B (examples + docs debt + behavioural tables + 2 fix rounds) both closed; `make ci` passes end to end at 81.5% coverage against the untouched 80% gate.
  - **Orchestrator's independent spot-checks for phase B, run directly:** (1) the two phantom alias builders are gone from the maps — the only remaining mentions are the historical "gone with the resource" note and this plan; (2) the balance-history example now names an instant and says why; (3) mutation spot-check on an ex-survivor — deleting the `PortfolioId` bridge line reddens exactly the accounts list row with the message naming the product defect ("a narrowing the caller set never reached the server"), restored and green, tree clean.
  - **What remains for the branch, outside the epics:** integration against live Midaz develop (the goal's final clause), branch rename `agent/` → `feat/` before the PR, PR title `feat!:` base `develop`. Known-open items recorded for GA, not this branch: onboarding→ledger rename; `/` and `%` id refusals named as breaking; V1 dual spellings revisit; `entities/observability.go` zero callers (pre-existing); the nine V2-only services absent from the workflow example (a feature, recorded in its README).

- [x] **Integration against live Midaz develop — PASSED** (2026-08-25, run by the orchestrator on mordor). Stack: worktree of midaz @ `ab01fc935` (current develop, NEWER than the `d816e289b` the SDK was reconciled against — no drift bit), `make up`, ledger on :3002 and tracer on :4020 both healthy.
  - **V1 surface, end to end via the mass-demo-generator** (`make demo-data`, `DEMO_TX_PER_ACCOUNT=20`, `DEMO_RUN_BATCH=true`): org + ledger + asset + default account types + 7 operation routes + 4 transaction routes + 10 accounts + portfolio + segments + hierarchy, then **200/200 transactions, 0 errors, 124.85 tps**, with the generator's own generated-vs-target assertion passing.
  - **V2 surface, end to end via a temporary smoke** (`itg-smoke`, deleted after the run; results recorded here): Organizations/Ledgers/Assets/Accounts Create → funding `@external/BRL` → alice 1000.00 → transfer alice→bob 250.00 → **balances read back as double entry demands: alice available=750, bob available=250** → List=2, `Count` with YYYY-MM-DD day bounds against the REAL server=2 (the widened date surface works live), Get returns amount/assetCode intact. All on `c.V2.*`.
  - **Two integration notes, no code change:** (1) the stock `.env.example` ships `DEMO_TX_PER_ACCOUNT=0` and `DEMO_RUN_BATCH=false`, so a stock `make demo-data` creates the onboarding tree but ZERO transactions — anyone "verifying with make demo-data" per the repo docs is not exercising the money path unless they flip both; deliberate-looking (don't spam a shared server), recorded rather than changed. (2) `examples/03-end-to-end` sends the placeholder ids `"org123"/"ledger456"`, so against a real server it dies with a 400 validation error (non-UUID path id) — the SDK surfaced the status and category correctly, which is itself the phase-A behavior working, but the example is illustrative-only and says nothing about it; candidate for a phase-C polish, not blocking.

- [ ] Epic 3 — **inputs from Epic 2:**
  - Every accessor is now a concrete facade pointer; no interface indirection remains anywhere, so adding a V2 twin is a pure addition to `V2Services` + a `newV2Services` line.
  - `V1.Balances` and `V1.Operations` are v1-only today, but the generated client ALREADY carries the complete v2 twin for all 13 of their operations (`getAllBalancesV2`, `getAllOperationsByAccountV2`, `updateOperationV2`, …) with no facade. Same for the other dual families.
  - `V1.Accounts.ListBalances` / `ListOperations` / `BalancesAtTimestamp` call the **same three endpoints** as `V1.Balances.ListAccountBalances` / `V1.Operations.ListOperations` / `V1.Balances.GetAccountBalancesHistory`. Both spellings work and both are tested. Whether both survive is a product call, not a code one.
  - `pkg/integrity` now reaches `V1.Balances` through a consumer-side one-method `balancesLister` interface, mirroring its existing `accountsGetter`. That is the pattern any downstream consumer needs now that the facades are unexported concrete types and the generated mocks are gone.
  - The generated params are ground truth per operation — three of the five model-shape corrections above came from reading them instead of a sibling facade's opts.

## Findings from Epic 3 (input for Epic 4 and the review round)

Everything below was read out of `internal/genledger` or `.references/midaz` @ `d816e289b`, or reproduced against a live `httptest.Server` through the public client.

**Contract surprises in the generated types — the reason "never copy a v1 sibling's shape" earned its place:**

- **`POST /v2/transactions/direct|hold|block|unblock` carry NO organization or ledger at the top level of the body.** The Epic-3 brief said "org/ledger travel in the BODY", which is true but not where anyone would look: they travel **per LEG**. `V2LegInput` requires `organizationId` and `ledgerId` on every debit and every credit, and the server folds them into one scope, refusing a body whose legs disagree (`pkg/mtransaction/v2_input.go:299`, `resolveScope`). The pair is therefore a property of the request body, and the URL carries none of it. **Decision:** the facade still takes `(orgID, ledgerID)` and stamps them onto every leg that leaves them empty, refusing a leg that names a different pair — the same reconcile-or-refuse rule the billing calculation already applies between its path ledger and its body `ledgerId`. Making a caller repeat the pair across twelve legs by hand is precisely the mistake the server rejects. The comparison is case-insensitive because the server's is; being stricter would refuse bodies the ledger accepts. The scoped copy is deep on both leg slices — stamping in place would mutate the caller's input, and a caller reusing one input across two ledgers would post the second transaction against the first.
- **All four v2 create actions share one request type** (`CreateTransactionV2JSONRequestBody = CreateTransactionV2Input`) and one response (`TransactionV2`). Nothing in the payload says which action runs; the endpoint does. The server folds the action into its own no-key idempotency identity so two byte-identical bodies posted to `/direct` and `/hold` cannot replay each other's result.
- **`getOperationByAccountV2` and `updateOperationV2` answer with the v1 `Operation` schema, NOT `OperationV2`.** `OperationV2` — which drops `chartOfAccounts` and `route` — appears ONLY nested inside `TransactionV2.Operations`. So `V2.Operations.*` returns `models.Operation` while `TransactionV2.Operations` carries `models.OperationV2`. That reads like an inconsistency because it is one, on the server's side; the SDK reports what each endpoint sends rather than papering over it. Worth raising with the ledger team.
- **`GetAllTransactionsV2` answers with `TransactionV2ListBody`, a typed envelope, not the shared untyped `Pagination`.** No behavioural consequence for the SDK (the facades decode the raw body into `models.ListResponse[T]` themselves), but it is the only list on the surface with its own envelope type.
- **`EstimateFeeCalculationV2` declares its 200 body as `*[]byte`** — opaque in the generated client. Already exposed and unchanged by this epic; noted because it is the one V2 read whose response shape the generated types do not describe.
- **Every dual-family V2 params struct is field-identical to its V1 twin**, verified field by field across all fourteen list/count operations rather than assumed. They remain distinct Go types, so `entities/v2_params.go` bridges them by copying the V1 mapper's output field by field. A whole-struct conversion would compile — Go allows it between structs with identical field types — and would silently map `start_date` onto `end_date` the day a regenerated client reorders two same-typed fields. Named fields make that a compile error.
- **`CountAccountsV2` exists but `CountAccountTypesV2` does not**, matching /v1. Neither route family has a count on either version.

**Deliberate decisions (orchestrator, 2026-08-25):**

- **V2 has ONE spelling per endpoint.** /v1 reaches the account-scoped balance and operation reads through both `V1.Accounts` and `V1.Balances`/`V1.Operations`, and the transaction-scoped operation update through both `V1.Transactions` and `V1.Operations`. Those duals cost something real: the two /v1 spellings of the point-in-time balance read enforced DIFFERENT date contracts until Epic 2 reconciled them. On /v2 each of those endpoints is spelled once, on the facade named after what it returns — `V2.Balances`, `V2.Operations`. `V2.Accounts` and `V2.Transactions` do not re-expose them. Pinned by `TestV2HasOneSpellingPerEndpoint` (negative) and `TestV2OwnsTheDeDuplicatedEndpoints` (positive, so deleting both spellings cannot pass as de-duplication). **Amended in the Epic 3 fix round:** the negative half originally listed method names, which a re-add under another name walks past; it now counts the V2 facades reaching each generated operation and requires exactly one.
- **V2 operation names are prefixed `V2.`** (`V2.Balances.GetBalance`) so an error names the surface that refused without the reader having to recognise the resource. V1 stays unprefixed; renaming it would churn every existing error string.
- **Nothing was left deliberately unexposed.** After this epic BOTH generated clients have ZERO operations without a facade — verified by enumerating every `New*Request` builder and matching it against the facade layer. The three v1 endpoints that had none are now exposed; the seven v1-only operations with no v2 twin (asset rates ×3, the four v1 creation styles) were already exposed and stay V1-only, which is what the server serves. **Re-verification note:** a naive `grep '<Op>('` reports ten tracer operations as unexposed. They are not — the rule, limit and reservation transitions are passed to their transition helpers as function VALUES, so the name appears without a call paren. Match on the bare name.

**Cross-cutting, from the brief:**

- **(a) DELETE 204 robustness — DONE, centrally.** Fixed at one seam rather than per facade. The generated DELETE parser unmarshals the body as an error whenever `Content-Type` contains `json`, so a bodiless 204 stamped with a JSON content type by any gateway made a SUCCESSFUL delete report `unexpected end of JSON input`, in every facade at once. All 16 ledger deletes now read the raw response. Tested against a live `httptest.Server` sending `204 + Content-Type: application/json` + empty body, through `New(WithBaseURL(...))`, across five path shapes on both surfaces, plus the two negative halves (a 409 problem document still surfaces with its detail; an empty 404 body still surfaces as not-found rather than as a decode fault).
- **(b) Non-UUID id in a single-object 2xx — PARTIALLY done, rest is Epic 4.** The root cause is the same generated parser: it decodes into the generated model whose ids are `openapi_types.UUID`, so a non-UUID id fails the unmarshal and the facade reports `errors.NewInternalError` — an SDK fault — for what is a decode or API problem. **Every new V2 facade is immune by construction**: they all read the raw response through `readOne`/`readList`/`readSlice`, which decode straight into the SDK model. The V1 surface still has it, at **45 remaining `*WithResponse` call sites** (82 before fix round 2, which moved every list onto the raw path; 48 before fix round 3, which moved the last three array reads — see round 2 finding 1 and round 3 finding 3). **CLOSED in Epic 4 phase A: all 45 retrofitted, and the 45 was confirmed by the scan before the work started — the one count in this epic that survived contact with a parser. Zero `*WithResponse` call sites remain on either plane.** The retrofit is mechanical and would SHORTEN the code — each 6-line block collapses to `return readOne[models.X](operation, f.ledger.GetX(ctx, ...))` — but it rewrites the read path of a money-path surface that has just come through three fix rounds, so it is recorded rather than bundled in. **Recipe for Epic 4:** replace `resp, err := f.ledger.OpWithResponse(...)` + the status/decode block with the matching `readOne` / `readList` / `readSlice` / `deleteResource` helper over the raw `f.ledger.Op(...)`, then extend `TestEveryDeleteRoutesThroughTheSharedSeam` into a general "no facade calls a generated `*WithResponse`" scan, which would make the retrofit self-verifying.
  - ~~**Fix round 3 deferral (MEDIUM-3), folded in here:**~~ **CLOSED in Epic 4 phase A — all 45 sites retrofitted, and the pin took the suggested shape: the three properties asserted over a seven-row table whose completeness comes from the scan rather than from the table.** Original text: the verifier measured the same class on all **45** remaining single-object sites and confirmed the worse half of it — a non-2xx carrying an EMPTY body (a gateway 403/404/502) loses the real status and arrives as `internal_error`/500, exactly as round 3 finding 3 proved on the three array reads before fixing them. So this backlog item is not only about a non-UUID id: **every one of the 45 sites can destroy an upstream error status.** That raises the retrofit's priority in Epic 4 from tidiness to correctness, and gives it a ready-made pin — the `bareArrayReads` table in `entities/empty_success_body_test.go` is the shape to copy, asserting 403 stays authorization and 404 stays not-found through an empty error body.

- ~~**Fix round 3 deferral (MEDIUM-2) — the path-id scan credits a vacuous guard.**~~ **Claimed CLOSED in Epic 4 phase A (`88b0503`) — the closure was FALSE, and it is closed as far as AST matching goes only after the Epic 4 phase A fix round (`5f3a45e`).** `88b0503` compared the guarded values against the path arguments actually forwarded, which was the right move and is intact; but it collected those values from the whole function body as source text, with no ordering and no reachability, so a guard placed BELOW the call, a guard nested inside an `if`, and the vacuous guard itself reached through a hoisted local all still passed. `5f3a45e` requires the guard at statement depth 1, unconditional, and positionally before the call, and follows a hoisted binding. Two escapes — a guarded variable reassigned before the call, and an inner scope shadowing a guarded name — remain open by decision and are documented in the scan as a known ceiling: closing them needs type-checked SSA, not a stricter match. Zero live instances tripped at either step; 209 credited functions throughout. Original text: `TestEveryPathParameterOperationIsGuarded` accepts a `requirePathIDs()` call that names NO pairs, so a facade could satisfy the scan with a guard that checks nothing. There are **zero live instances** today — every guarded call site passes real pairs — which is why it was recorded rather than fixed mid-round: it is a hole in the proof, not a defect in the product. **Fix for Epic 4:** stop treating the presence of the call as the property and compare the values the guard receives against the path arguments actually forwarded to the generated operation, which is the identity-checking approach the Epic-2 contrarian pass argued for. Same file as the seam scan the round-2 HIGH hardened, so it belongs with that work.

**Method carried forward from Epic 2, and it paid twice:** where an invariant spans a generated surface, derive the expectation from the generated code at test time instead of hand-maintaining a table. The new idempotency scan found a real hole in this epic's own code the moment it was written (the four v2 creates resolve their key in a shared helper, so nothing in their own bodies did — the same unverified-promise shape the path-id scan had caught), and it surfaced nine pre-existing tracer writes that carry no key at all. Those nine are legitimate and are now recorded with reasons instead of being invisible.

**Latent, NOT introduced here, for the review round:**

- Nine tracer writes carry no idempotency key at all: the three rule transitions, the three limit transitions, and three reservation transitions (confirm, confirm-by-transaction, release-by-transaction). The rule and limit transitions are config and move no balance. The three reservation ones are money-ADJACENT — they confirm or release held value — and are keyed only by the id in their path. That is the same reasoning the ledger's commit/cancel/revert rely on, and those at least carry a caller-supplied key when one is offered; the reservation transitions carry none because `reservationsFacade` has no idempotency gate at all. Recorded, not changed: the tracer plane is outside this epic's scope.
- `entities/observability.go` still has zero callers (unchanged, Epic 2 finding).

- [ ] Epic 4 — **doc debt inventory (beyond the prose already scoped):** `docs/mapping/external_apis.md` (lines ~236, 279, 532, 534) and `docs/mapping/internal_apis.md` (~20, 95, 96) still document `BalancesService` / `OperationsService` / `AliasesService`, `BalancesFilters` and `AliasesListOpts`; `docs/examples.md:112` too; `docs/godoc/**` is generated and stale. `README.md` was updated in Epic 2 (accessor tree, quickstart, and the mocks claim — `entities/mocks/` no longer exists) because it carried runnable code that no longer compiled; the surrounding prose in `docs/` is still Epic 4's.

---

## Phase C (2026-08-25) — the generator becomes the live-integration proof, and it caught two shipped defects

**Trigger:** Fred asked whether expanding the mass-demo-generator beat writing throwaway smokes. It did: the generator is now the permanent, reproducible live proof of BOTH surfaces (`make demo-data` against a stock `make up` stack), and its first run against midaz develop @ ab01fc935 exposed two V2 Instruments defects that behavioural tests against a permissive stub had let through.

**What the generator does now (gate `DEMO_RUN_V2`, default on, per ledger, after the v1 batch):**
- CRM: 5 holders, holder-owned accounts via composition, ONE direct `V2.Instruments.Create` against an instrument-free base account (the server enforces one instrument per account — its 409 told us), instruments/holder-accounts reads.
- **A FATAL money proof:** fund 100 + 50 from `@external`, direct transfer 30, hold 20 + commit, hold 10 + cancel, then exact-balance assertion (source 50 / dest 100, nothing on hold) with a bounded poll that can only retry-then-fail-loudly — expectations are computed once and never adjusted from observations. Proof quantities live in shared constants referenced by both the production assertion and the unit test.
- One fee package + estimate; one billing package + calculation (ledger-scoped); metadata-index create/list/delete once per run (global resource); encryption/protection-audit read probes that tolerate deployments without envelope encryption mode, with feature-not-available reported distinctly from transport failure.
- Stock env templates now exercise money paths (`DEMO_TX_PER_ACCOUNT=5`, `DEMO_RUN_BATCH=true`); reports generate when either phase ran; root-level run artifacts are gitignored.

**The two live-exposed SDK defects, both fixed (`51e27e3`), neither ever worked so nothing breaking-in-practice:**
1. `CreateInstrumentInput` carried two phantom fields (`type` — required by its own Validate —, `document`) that `additionalProperties:false` rejects, and lacked the contract-required `ledgerId`/`accountId`. `UpdateInstrumentInput` had the same `document` phantom; its null-fields now refuse clearing contract-required properties. The wire stubs now mirror `additionalProperties:false` + required sets and assert exact bodies — the permissive-stub hole that let this ship is closed as a class for this family.
2. `ListAccountsByHolder`/Pages/All 400'd on every live call: the server REQUIRES query param `ledger_id` (midaz `holder_wiring.go:62-70`, deliberate) but the published contract omits it, so codegen never produced the slot. The facade now takes `ledgerID` and injects the param manually, with the drift documented at the injection site. **Upstream item for the ledger team:** org-scope `GET /v2/organizations/{org}/holders/{id}/accounts` enforces an undocumented required query param; the OAS and runtime disagree inside the same binary.

**Live verification (three runs against midaz develop @ ab01fc935, stack stock `make up`, torn down after):** run 1 green with 3 tolerated skips (two expected: encryption/protection routes only exist in envelope encryption mode; one real: the ledger_id 400). Run 3, after fixes: 100% green — direct instrument create 201, holder-accounts listing live, hold-release accounting proven (cancel returned its 10). Plus a **deliberate failure test**: expectation corrupted by one minor unit → run exits non-zero with "expected available=50.01, last read available=50" → reverted (byte-identical, test-cache hit proves it). The proof's failure direction is now live-proven, closing the reviewer's residual risk #2; residual #3 (Cancel) closed by the hold-release leg.

**Review:** one opus review round (money proof CLEAN — poll cannot mask, nothing-on-hold asserted, fatal path proven to os.Exit(1)), 10 findings, all fixed in one round + the Update defect the fix agent itself flagged. `make ci` green cold-cache end to end. HEAD now `1be9a37` (`51e27e3` fix(instruments) + `1be9a37` feat(examples)), tree clean, NOT pushed.

**Still latent, recorded:** instrument identifiers validated as UUIDs at Validate() but typed string (consistent with every body-carried id in the SDK); the proof runs at one asset scale per run (scale 0/6 covered by unit arithmetic only); `V2.Instruments.Update`'s required-on-PATCH contract is the server's choice, mirrored not editorialized.
