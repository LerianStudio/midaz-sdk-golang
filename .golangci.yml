version: "2"
run:
  tests: true
  timeout: 5m
linters:
  enable:
    # ===========================================
    # ERROR HANDLING (Critical for SDK reliability)
    # ===========================================
    - bodyclose       # HTTP response body must be closed
    - errcheck        # Check error returns
    - errchkjson      # Check JSON encoding errors
    - nilerr          # Returning nil after checking err != nil
    - errorlint       # Proper error wrapping with %w

    # ===========================================
    # SECURITY (Essential for financial SDK)
    # ===========================================
    - gosec           # Security-focused linting
    - noctx           # HTTP requests must use context

    # ===========================================
    # CODE QUALITY & COMPLEXITY
    # ===========================================
    - gocognit        # Cognitive complexity
    - gocyclo         # Cyclomatic complexity
    - funlen          # Function length limits
    - nestif          # Nested if limits
    - cyclop          # Alternative complexity checker
    - maintidx        # Maintainability index

    # ===========================================
    # CORRECTNESS & BUG PREVENTION
    # ===========================================
    - staticcheck     # Comprehensive static analysis
    - govet           # Go vet checks
    - ineffassign     # Ineffective assignments
    - wastedassign    # Wasted assignments
    - reassign        # Reassignment detection
    - copyloopvar     # Loop variable issues (Go 1.22+)
    - nilnil          # Returning nil, nil
    - predeclared     # Shadowing predeclared identifiers
    - makezero        # Slice initialization issues
    - durationcheck   # Duration multiplication issues

    # ===========================================
    # CODE STYLE & CONSISTENCY
    # ===========================================
    - revive          # Replacement for golint
    - misspell        # Spelling mistakes
    - dupword         # Duplicate words
    - unconvert       # Unnecessary conversions
    - goconst         # Repeated strings/constants
    - mnd             # Magic number detection
    - usestdlibvars   # Use stdlib variables (http.MethodGet, etc.)
    - dogsled         # Too many blank identifiers
    - nakedret        # Naked returns
    - prealloc        # Preallocate slices
    - nolintlint      # Proper nolint directives

    # ===========================================
    # CODE ORGANIZATION
    # ===========================================
    - depguard        # Dependency management
    - gomoddirectives # Go mod directives

    # ===========================================
    # PATTERN ENFORCEMENT
    # ===========================================
    - forbidigo       # Forbid specific function calls

    # ===========================================
    # TESTING QUALITY
    # ===========================================
    - thelper         # Test helpers must call t.Helper()
    - tparallel       # Parallel test issues
    - testifylint     # Testify best practices

    # ===========================================
    # PERFORMANCE
    # ===========================================
    - loggercheck     # Logger key-value pairs
    - unparam         # Unused parameters

  settings:
    # -----------------------------------------
    # Complexity Settings
    # -----------------------------------------
    gocyclo:
      min-complexity: 15
    gocognit:
      min-complexity: 20
    funlen:
      lines: 80
      statements: 40
    nestif:
      min-complexity: 4
    cyclop:
      max-complexity: 15
    maintidx:
      under: 20

    # -----------------------------------------
    # Error Handling Settings
    # -----------------------------------------
    errcheck:
      check-type-assertions: true
      check-blank: true
      exclude-functions:
        - io.Copy
        - (io.Closer).Close
        - (net/http.ResponseWriter).Write
    errorlint:
      errorf: true
      errorf-multi: true
      asserts: true
      comparison: true

    # -----------------------------------------
    # Code Style Settings
    # -----------------------------------------
    goconst:
      min-len: 3
      min-occurrences: 10
      ignore-tests: true
    mnd:
      checks:
        - condition

    # -----------------------------------------
    # Security Settings
    # -----------------------------------------
    gosec:
      excludes:
        - G104  # Unhandled errors (covered by errcheck)
        - G304  # File path from variable (needed for SDK)

      config:
        global:
          audit: false

    # -----------------------------------------
    # Pattern Enforcement (forbidigo)
    # -----------------------------------------
    forbidigo:
      # Global setting to exclude godoc examples from pattern matching
      exclude-godoc-examples: true
      analyze-types: true
      forbid:
        # Forbid fmt.Print* in non-example code (use structured logging)
        - pattern: '^fmt\.Print.*$'
          msg: "Use structured logging via pkg/observability instead of fmt.Print"

        # Forbid panic in library code
        - pattern: '^panic$'
          msg: "Do not use panic() in library code - return errors instead"

        # Forbid os.Exit in library code
        - pattern: '^os\.Exit$'
          msg: "Do not use os.Exit() in library code - return errors instead"

        # Forbid time.Sleep in non-test code (use context with timeout)
        - pattern: '^time\.Sleep$'
          msg: "Avoid time.Sleep - use context with timeout or time.After with select"

        # Forbid ioutil (deprecated)
        - pattern: '^ioutil\.'
          msg: "ioutil is deprecated - use io and os packages instead"

        # Forbid log package (use structured logging via log/slog or
        # pkg/observability). The broad `^log\.` pattern catches every
        # stdlib log call (Print/Fatal/Panic, plus New/SetOutput/Default/
        # Writer/etc.) without affecting log/slog calls, which are
        # written `slog.X(...)` and never `log.X(...)`.
        - pattern: '^log\.'
          msg: "Use log/slog or pkg/observability instead of the stdlib log package"

        # Forbid http.Get/Post without context
        - pattern: '^http\.(Get|Post|Head)$'
          msg: "Use http.NewRequestWithContext instead for proper context propagation"

        # Forbid unsafe package
        - pattern: '^unsafe\.'
          msg: "Avoid unsafe package in SDK code"

    # -----------------------------------------
    # Dependency Guard
    # -----------------------------------------
    # v3 contract: log/slog (stdlib structured logging) is the canonical
    # logging surface — see docs/logging.md and Track 4 of the v3 DX plan.
    # The v1 stdlib `log` package is no longer denied here because depguard
    # matches deny entries by prefix; banning `log` would also ban `log/slog`.
    # If v1 log usage creeps in, code review and the slog-everywhere idiom
    # will catch it.
    depguard:
      rules:
        main:
          deny:
            - pkg: io/ioutil
              desc: "Deprecated: Use io and os packages instead (Go 1.16+)"
            - pkg: github.com/pkg/errors
              desc: "Use standard errors package with fmt.Errorf and %w"

    # -----------------------------------------
    # Go Vet
    # -----------------------------------------
    govet:
      enable-all: true
      disable:
        - fieldalignment
        - inline        # Too noisy for reflect-heavy conversion helpers.
      settings:
        shadow:
          strict: true

    # -----------------------------------------
    # Revive Rules (Comprehensive)
    # -----------------------------------------
    revive:
      severity: warning
      enable-all-rules: false
      rules:
        # Critical - must fix
        - name: import-shadowing
          severity: error
        - name: context-as-argument
          severity: error
        - name: context-keys-type
          severity: error
        - name: error-return
          severity: error
        - name: range-val-in-closure
          severity: error
        - name: range-val-address
          severity: error
        - name: unreachable-code
          severity: error
        - name: struct-tag
          severity: error
        - name: constant-logical-expr
          severity: error
        - name: redefines-builtin-id
          severity: error
        - name: unconditional-recursion
          severity: error
        - name: identical-branches
          severity: error
        - name: datarace
          severity: error
        - name: waitgroup-by-value
          severity: error
        - name: atomic
          severity: error
        - name: string-of-int
          severity: error

        # Important - should fix
        - name: empty-block
          severity: warning
        - name: empty-lines
          severity: warning
        - name: use-any
          severity: warning
        - name: error-strings
          severity: warning
        - name: error-naming
          severity: warning
        - name: exported
          severity: warning
          arguments:
            - checkPrivateReceivers
            - sayRepetitiveInsteadOfStutters
        - name: if-return
          severity: warning
        - name: increment-decrement
          severity: warning
        - name: var-naming
          severity: warning
        - name: var-declaration
          severity: warning
        - name: receiver-naming
          severity: warning
        - name: range
          severity: warning
        - name: indent-error-flow
          severity: warning
        - name: superfluous-else
          severity: warning
        - name: modifies-parameter
          severity: warning
        - name: bool-literal-in-expr
          severity: warning
        - name: imports-blocklist
          severity: error
        - name: redundant-import-alias
          severity: warning
        - name: unnecessary-stmt
          severity: warning
        - name: deep-exit
          severity: warning
        - name: unused-parameter
          severity: warning
        - name: unused-receiver
          severity: warning
        - name: unhandled-error
          severity: warning
          arguments:
            - fmt.Printf
            - fmt.Println
            - fmt.Fprintf
            - strings.Builder.WriteByte
            - strings.Builder.WriteString
        - name: cognitive-complexity
          severity: warning
          arguments: [20]
        - name: early-return
          severity: warning
        - name: defer
          severity: warning
          arguments:
            - ["call-chain", "loop"]
        - name: unexported-naming
          severity: warning
        - name: unexported-return
          severity: warning
        - name: useless-break
          severity: warning
        - name: confusing-results
          severity: warning
        - name: modifies-value-receiver
          severity: warning
        - name: time-equal
          severity: warning
        - name: time-naming
          severity: warning
        - name: optimize-operands-order
          severity: warning
        - name: use-errors-new
          severity: warning

    # -----------------------------------------
    # Whitespace Settings
    # -----------------------------------------
    wsl_v5:
      allow-first-in-block: true

    # -----------------------------------------
    # Testing Settings
    # -----------------------------------------
    testifylint:
      enable-all: true

  # ===========================================
  # EXCLUSIONS
  # ===========================================
  exclusions:
    generated: lax
    rules:
      # Relax rules for test files
      - linters:
          - errcheck
          - gosec
          - funlen
          - gocognit
          - gocyclo
          - cyclop
          - maintidx
          - dupl
          - forbidigo
          - goconst
          - mnd
          - noctx
        path: _test\.go

      # Allow complexity in generator
      - linters:
          - funlen
          - goconst
          - gocognit
          - gocyclo
          - mnd
          - maintidx
        path: pkg/generator/

      # Allow complexity in data package
      - linters:
          - funlen
          - goconst
          - gocognit
          - mnd
        path: pkg/data/

      # Allow fmt.Print and log in examples
      - linters:
          - forbidigo
          - depguard
          - goconst
          - mnd
        path: examples/

      # Allow fmt.Print in main packages (CLI tools)
      - linters:
          - forbidigo
        path: cmd/

      # Existing exclusions
      - linters:
          - errcheck
        text: Error return value of .((os\.)?std(out|err)\..*|.*Close|.*Flush|os\.Remove(All)?|.*print(f|ln)?|os\.(Un)?Setenv). is not checked
      - linters:
          - revive
        text: func name will be used as test\.Test.* by other packages
      - linters:
          - revive
        text: "package-comments: should have a package comment"
      - linters:
          - staticcheck
        text: "ST1000: at least one file in a package should have a package comment"
      - linters:
          - govet
        text: '^shadow: declaration of "(err|ok)" shadows declaration'
      # Allow utils as package name (common convention for utility packages)
      - linters:
          - revive
        text: "var-naming: avoid meaningless package names"
        path: pkg/utils/

    paths:
      - third_party$
      - builtin$
      - .repos$
      - mocks$

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  new: false
  fix: false

formatters:
  enable:
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
      - .repos$
